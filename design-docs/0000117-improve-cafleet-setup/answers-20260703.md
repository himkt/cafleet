User answers to the Drafter's clarifying questions (design 0000117 improve-cafleet-setup):

Q1 — DB backend: Complete the 0000111 hard cut (your option a). `setup db` runs the single-baseline metadata create; delete the Alembic chain (0001-0005), alembic.ini, and the upgrade-guard machinery. The new version table becomes part of the baseline.

Q2 — Version table shape: One row per coding-agent home (your option a). Keyed by coding_agent (claude/codex/opencode) with cafleet_version and installed_at. Keep it minimal — no extra columns requested.

Q2 addendum — CLI surface change requested by the user: REMOVE the --agent option from bare `cafleet setup`. Bare `cafleet setup` installs skills for ALL coding agents. `--agent` lives ONLY on `cafleet setup skill` (to target a single home).

Q3 — Version check enforcement: HARD ERROR broadly + doctor detail (user picked a custom option "Error broadly + doctor detail"). Fleet-scoped commands (fleet create, member create, ...) refuse to run on a version mismatch until skills are reinstalled; `cafleet doctor` reports the full detail. Defaults accepted: mismatch is simple string inequality (a downgrade also triggers), and an empty table / pre-feature DB is treated as "setup never ran" (same treatment).

Q4 — Recording semantics: Your proposed ordering is confirmed — bare `cafleet setup` runs the db half first, then the skills half; halves stay independent-failure. `setup skill` records/updates the version row on success. When the DB/schema does not exist or is not up to date, `setup skill` FAILS WITH GUIDANCE ("run cafleet setup first") — it does NOT auto-create the schema. `setup db` never touches the version row.

Q5 — CLI surface details: Confirm all. `cafleet setup` becomes a Click group whose bare invocation runs everything; subcommand named `skill` (singular); `setup db` takes no options; the `cafleet db` group is removed hard — no alias, no deprecation notice, every mention swept (CLAUDE.md, SPEC.md, docs, admin Dashboard.tsx error hint, tests) per the repo removal rule.

Q6 — Testing: As assumed. Rework tests/cli/test_setup.py and tests/db/test_init.py to the new surface (`setup db` / `setup skill` / version-record + mismatch-check tests); delete Alembic-specific tests.

Final confirmed surface:
- `cafleet setup` = do everything (db first, then skills for all coding agents)
- `cafleet setup db` = only the DB migration (schema create)
- `cafleet setup skill [--agent <name>]` = only the skill download; errors with guidance when the schema is missing/outdated; records the version row on success

You now have everything you need — proceed with writing the design document at your OUTPUT PATH.
