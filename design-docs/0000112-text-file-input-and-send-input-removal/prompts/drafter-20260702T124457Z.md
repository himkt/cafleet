You are the Drafter in a design document creation team (CAFleet-native).

ROLE DEFINITION: Open /Users/himkt/.claude/skills/cafleet-design-doc/create/roles/drafter.md with the Read tool BEFORE any other action. That file is your authoritative role definition. Re-read it whenever you are unsure of protocol.

Load these skills at startup:
- the cafleet skill — for communication with the Director
- the cafleet-design-doc skill — for the design-doc template and guidelines

FLEET ID: $CAFLEET_FLEET_ID
DIRECTOR AGENT ID: $CAFLEET_DIRECTOR_AGENT_ID
YOUR AGENT ID: $CAFLEET_AGENT_ID
BASE: /Users/himkt/work/himkt/cafleet/design-docs/0000112-text-file-input-and-send-input-removal
CODING AGENT: claude

OUTPUT PATH: /Users/himkt/work/himkt/cafleet/design-docs/0000112-text-file-input-and-send-input-removal/design-doc.md

The user's request: Design a change to CAFleet's CLI text-input surface, motivated by GitHub issue #153 ("message send cannot accept long bodies" — --text is inline-only and hits the shell's ARG_MAX). ALL of the decisions below are ALREADY MADE by the user; the design doc specifies them and does NOT re-open them. Your clarifying questions should be a focused confirmation round, not a re-litigation.

SCOPE:
1. UNIFIED TEXT-INPUT PAIR on all four text-body commands: `message send`, `message broadcast`, `member nudge`, `member create`. Each exposes exactly: `--text <str>` (inline body) and `--text-file <path>` (read body from file; absolute OR relative path; UTF-8; `--text-file -` reads from stdin). They are mutually exclusive; exactly one is required. A single shared reader helper is the source of truth for mutual-exclusivity validation, path resolution (abs/rel), UTF-8 read, stdin (`-`) handling, and error messages. Today's helper lives in cafleet/src/cafleet/cli/_prompt.py (read_prompt_file / resolve_prompt) — rename/refactor it into one generic text-input helper used by all four commands.
2. member create specifics (the "unify" consequence): remove `--prompt-file` (HARD rename to `--text-file`, NO alias, per .claude/rules/removal.md); add `--text`; and REMOVE the positional prompt_argv argument so member create's surface matches the other three commands exactly.
3. REMOVE `cafleet member send-input` entirely. Rationale: every member spawns with unconditional auto-approve flags on all three backends — claude `--permission-mode dontAsk` (which DENIES the AskUserQuestion tool outright), codex `--ask-for-approval never --sandbox workspace-write`, opencode `--agent cafleet` preset — and codex/opencode have no in-pane prompt surface at all. So the AskUserQuestion 4-option pane frame that send-input targets is structurally unreachable by any member; it is dead code. Removal surface to enumerate in the doc: the command (cafleet/src/cafleet/cli/member.py, the member_send_input command ~lines 535-604), the tmux helpers send_choice_key and send_freetext_and_submit (cafleet/src/cafleet/multiplexer/tmux.py + base.py — verify no other callers before removing), the --choice/--freetext options, all send-input tests (cafleet/tests/cli/test_member_send_input.py, cafleet/tests/multiplexer/test_tmux_send_helpers.py, cafleet/tests/multiplexer/test_tmux_send_inline_preview.py, and send-input slices of test_tmux.py / test_help_budget.py / test_member_delete.py), SPEC.md, docs/spec/cli-options.md (### member send-input), docs/how-to/monitor-and-recover.md, the skills/cafleet/reference/director.md "Answering a member's relayed question" / send-input references, the send-input 4-option-frame caveats in the coding-agent overlays (skills/cafleet/reference/coding-agent/*.md), any send-input mentions in the monitoring/supervision skills, and any .claude/settings.json permissions.allow patterns. Per removal.md: delete every mention cleanly with NO deprecation notices; design docs 0000027 / 0000033 / 0000098 remain as the historical record and are NOT edited.
4. SKILLS ripple (the --prompt-file rename touches ~15 files — confirm the live set with Grep): update the member-spawn recipes (skills/cafleet-design-doc/create/create.md, execute/execute.md, interview/interview.md, skills/cafleet-research/report/report.md, presentation/presentation.md), skills/cafleet/reference/director.md, skills/cafleet/reference/supervision.md, and .claude/skills/skill-author/SKILL.md to use `--text-file`. ADDITIONALLY add explicit guidance in the relevant skills that long or multi-line bodies should be passed via `--text-file` (or stdin) rather than `--text`, to avoid ARG_MAX. NOTE: cafleet's own agent spawn `--prompt-file` flag is a SEPARATE flag from the CLI text commands — the design doc must decide and state explicitly whether `agent spawn --prompt-file` is also renamed or left as-is (recommend: analyze whether it shares the helper; the issue is about message/nudge bodies, and agent-spawn prompt files are a distinct concept — but the ~15-file ripple count above assumes the message-command flags; verify which mentions are agent-spawn vs message-command).
5. EXPLICITLY OUT OF SCOPE (state in the doc, with reasons): (a) the ~200-char broker message preview truncation cited in issue #153 — it is display-only (design 0000043-message-text-truncation); the full body is always persisted in SQLite and delivered in full; it is NOT a payload limit, so --text-file already solves the real problem; no behavior change to the preview. (b) POST /api/messages/send already accepts long bodies via the JSON request body (no ARG_MAX) — no change. (c) cafleet member exec is a separate single-line control input — unaffected.
6. This is a breaking CLI change (removed send-input, removed --prompt-file, removed member create positional arg) but the project is pre-1.0 (currently 0.14.0), so breaking changes are acceptable; note it in the doc.

Follow the project's documentation-first implementation order (docs/concepts, docs/, README.md, SPEC.md, every affected SKILL.md, then code). Use the design-doc template and guidelines from the cafleet-design-doc skill.

INSTALLED CLI NOTE: This environment's cafleet binary uses the `member *` command surface, not the `agent *` / `pane *` names some skill docs use. You communicate via `cafleet message send` / `cafleet message poll` / `cafleet message ack` (unchanged). If any command errors with "No such command", re-run its parent with --help and use the installed name.

IMPORTANT: You MUST ask clarifying questions BEFORE writing any design document file.
Send your questions to the Director who will relay them to the user.
Start by reading the target codebase for context, then send your clarifying questions.
Do NOT create any design document file until you have received answers.
