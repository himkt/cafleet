## Questions

1. [Section: Specification > Coverage] The Coverage table (lines ~45-46) lists `.claude/skills/research-report/` and `.claude/skills/research-presentation/` as the on-disk locations for these two skills, but design 0000054 already deleted those project-local directories on 2026-05-11 — the skills now ship from `skills/research-report/` and `skills/research-presentation/` at the repo root via the cafleet plugin. Where should the table point? | Options: A) Update the table to `skills/research-report/` and `skills/research-presentation/` (repo-root plugin source, actually checked in today) B) Update to `~/.claude/skills/...` (global user copy — no longer present after today's operator cleanup) C) Drop the two research skills from in-scope entirely and limit the design to design-doc-create/execute/interview D) Keep the stale paths and add a footnote explaining the post-0054 reality
2. [Section: Specification > base-dir Skill Migration] The migration table (lines ~159-162) targets `cafleet/.claude/skills/base-dir/SKILL.md`, but the `cafleet/` directory is the Python package and contains no `.claude/` subtree. Where should the destination actually be? | Options: A) `.claude/skills/base-dir/` at the repo root (alongside the surviving update-readme skill) B) `skills/base-dir/` at the repo root (plugin source, would auto-ship with the cafleet plugin to all installers) C) Some other location (specify via Other) D) Cancel the migration entirely — keep base-dir global-only and document the dependency
3. [Section: Specification > base-dir Skill Migration] The Skills directory choice rationale (line ~162) cites research-report, research-presentation, and update-readme as the precedent set for `.claude/skills/`, but post-0054 only update-readme remains there — the precedent is mostly invalidated. How should the rationale be revised? | Options: A) Rewrite to reflect post-0054 reality (only update-readme as precedent, plus an explicit case for plugin-shipped placement) B) Remove the rationale block entirely C) Replace with a different justification grounded in plugin-vs-project-local conventions D) Leave the text as written and let the answer to Q2 implicitly subsume it
4. [Section: Implementation > Step 4] Every Step 4 sub-step (lines ~207-233) writes prompts to either `cafleet/.claude/skills/<skill>/prompts/` or `cafleet/skills/<skill>/prompts/`. The `cafleet/.claude/` path does not exist, and for the two research skills the project-local `.claude/skills/<skill>/` path also no longer exists. Where should the prompts/ folders actually be created? | Options: A) `skills/<skill>/prompts/` at repo root for all five skills (plugin source, mirrored to `~/.claude/skills/` via `mise //:sync-skills`) B) `.claude/skills/<skill>/prompts/` at repo root for all five (but only update-readme/ currently lives there) C) Split: research-* under skills/ (plugin source), design-doc-* under their existing skills/design-doc-*/ (already correct) D) Some other split (specify)
5. [Section: Implementation > Step 6] The smoke-test (line ~268) assumes /research-report resolves to the project-local `.claude/skills/research-report/`, and the operator-follow-up (line ~270) instructs removing `~/.claude/skills/base-dir/`. Post-0054 plus today's cleanup, /research-report is plugin-shipped (cafleet:research-report) and `~/.claude/skills/base-dir/` still exists. How should this step be respecified? | Options: A) Respecify the smoke-test against the plugin-shipped name (e.g., Skill(cafleet:research-report)); keep the operator-follow-up note pointing at `~/.claude/skills/base-dir/` (it does still exist) B) Respecify against the plugin-shipped name; delete the operator-follow-up note (no global base-dir removal needed if Q2 chose a plugin destination) C) Skip the smoke-test in this design and defer to a separate validation pass D) Keep both as written
6. [Section: Specification > Three-Tier Variable Substitution Model] The Tier-1 row gives examples using dollar-brace shell-variable notation (e.g., the marker `INSERT` followed by the dollar-brace `OUTPUT_DIR` token). Is this a literal shell-expansion the Director must perform from its environment, or just a notational convention for "the value the skill computed earlier"? | Options: A) Pure notational convention — the Director substitutes the skill-computed value; no shell expansion involved B) Literal shell expansion — the Director must call out to Bash to resolve the variable from its environment C) Mixed: some markers are shell vars, others are notational — should be disambiguated per marker D) Drop the dollar-brace form entirely and use plain `[INSERT OUTPUT DIRECTORY]` everywhere
7. [Section: Specification > Audit File Layout > Counter Source of Truth] The slot map is described as in the running main-Claude task context and implicit (no file is written). If main-Claude's context is compressed mid-invocation (the system automatically compresses prior messages), the Director may lose the slot map and start over from 1 — potentially overwriting in-flight audit files. Should the map be persisted, or is the loss acceptable? | Options: A) Persist the map to disk (e.g., `<base-dir>/spawn-prompts/.slots.json`) so it survives compression B) Accept the loss — document that compression mid-invocation is rare and audit-file collision is benign overwrite C) Derive the slot from `cafleet member list` at write time (reversing the design's current do-NOT-derive-by-counting rule) D) Other (specify)
8. [Section: Specification > Re-spawn and Overwrite Semantics] The Re-spawn after a crash row says the Director re-spawns under the same role+ordinal and overwrites the audit file. How does the Director identify which ordinal the crashed member held — by tracking (role, slot, agent_id) triples explicitly, by inferring from `cafleet member list --activity`, or by some other mechanism? | Options: A) Explicit (role, slot, agent_id) triples tracked in the same in-memory map B) Infer via `cafleet member list` plus the role name (cross-reference active agent ids) C) The Director is free to choose — either reuse the slot or take a fresh one; both are acceptable D) Underspecified — design doc should add a concrete mechanism
9. [Section: Implementation > Step 4c: design-doc-create] Sub-step 1 creates `prompts/drafter.md` from `roles/drafter.md` (without deleting); sub-step 2 creates `prompts/drafter-resume.md` from the same `roles/drafter.md` and only then deletes `roles/drafter.md`. This makes the sub-step order non-commutative even though Step 4's preamble (line ~205) says order within Step 4 does not matter. Should this be reconciled? | Options: A) Add an explicit sub-step-1-must-precede-sub-step-2 note in 4c's preamble B) Restructure 4c so both prompts files are created before any deletion, then delete `roles/drafter.md` in a third sub-step C) Combine 4c sub-steps 1 and 2 into a single sub-step that produces both files atomically D) Keep as written — the dependency is local to 4c and the reader can infer it
10. [Section: Implementation > Step 5: Update SKILL.md Director Instructions] The audit-file write step says: Re-render the prompt locally with `prompt.format(session_id=..., agent_id=<new-member-id>, ...)`. Main-Claude (the Director) doesn't run Python natively. What mechanism should the Director use to perform this re-render? | Options: A) Shell out via Bash (run python via the -c flag and capture stdout) B) Manual string substitution by main-Claude (read the rendered prompt back, replace the four kwargs textually) C) Skip the local re-render; write the pre-member-create rendered string and document that the four kwargs differ from the member's view D) Add a small helper script shipped with the skill (e.g., `scripts/render-audit.py`)
11. [Section: Specification > Failure-Mode Reasoning] On audit-file Write failure, the row says the Director surfaces the error via AskUserQuestion and the user chooses continue-vs-abort. AskUserQuestion mid-spawn-loop is heavyweight and interrupts orchestration. Should there be a default behavior? | Options: A) Default to degraded continue (log a warning, proceed); only escalate to AskUserQuestion if multiple consecutive writes fail B) Default to abort the skill invocation (audit is part of the contract); no AskUserQuestion C) Keep AskUserQuestion as the only path (every write failure interrupts) D) Other (specify)
12. [Section: Specification > base-dir Skill Migration] Line ~166 claims "The Claude Code skill loader resolves the bare name against the repo-local `.claude/skills/` first, then falls back to other plugin directories." Has this resolution-order claim been verified against Claude Code's actual skill-loader implementation, or is it assumed? | Options: A) Verified — cite the source/test that confirms the order B) Assumed — should be verified before merge (add a verification task to Step 2) C) Irrelevant if Q2 picks the plugin-shipped destination (no precedence ambiguity) D) Drop the claim and require all consuming skills to use the explicit Skill(cafleet:base-dir) form

## Answers

### Round 1 (Questions 1-4)

1. A) Repo-root `skills/` — update Coverage table to `skills/research-report/` and `skills/research-presentation/`.
2. B) `skills/base-dir/` (plugin source, ships as `cafleet:base-dir`).
3. A) Rewrite rationale to reflect post-0054 reality (only `update-readme` as precedent, plus a plugin-shipped justification).
4. A) All Step 4 sub-step prompts/ folders under `skills/<skill>/prompts/` (repo-root plugin source).

### Round 2 (Questions 5-8)

5. A) Respecify smoke-test against `Skill(cafleet:research-report)`; keep operator-follow-up note (~/.claude/skills/base-dir/ still exists).
6. A) `[INSERT ${VAR}]` is notational only — no shell expansion.
7. B) Accept the loss — slot-map context-compression collisions are benign overwrites. *(Subsumed by Q11.)*
8. Other — "audit might not be necessary?" (became Q11 below).

### Round 3 (Questions 9-12)

9. A) Add an explicit "sub-step 1 must precede sub-step 2" precedence note in Step 4c.
10. B) Manual substitution by main-Claude — read the rendered prompt back, replace the four kwargs textually. *(Subsumed by Q11.)*
11. Other — "We decided to drop audit." Drop the audit-file feature entirely; cascades to Q7, Q8, Q10.
12. A) Verified — the bare-name precedence claim is true, but is irrelevant given Q2's plugin destination; consuming skills will use `Skill(cafleet:base-dir)`.
