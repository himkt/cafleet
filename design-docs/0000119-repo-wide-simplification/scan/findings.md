# Repo-wide simplification scan — verified findings

Produced by a 20-agent scan (10 area scanners + 10 adversarial verifiers), 2026-07-04.
Total findings: 66; confirmed: 49; refuted/rescoped: 17.

Every finding claims to be strictly behavior-preserving. "REFUTED/RESCOPED" findings were refuted or rescoped by the adversarial verifier — the verifier notes state why (often: the change is valid only if SPEC.md / docs/ edits are included in the same change).

## Area: admin-ui (4/5 confirmed)

### [admin-ui-1] Dead 204 handling branch in the API client — no /api endpoint ever returns 204
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `admin/src/api.ts:38-40`
- **Estimated impact**: -3 lines
- **Evidence**: admin/src/api.ts:38-40: `if (resp.status === 204) { return undefined as T; }`. The admin UI only talks to `webui_router` (the sole router included in cafleet/src/cafleet/webui/app.py:44). Every endpoint in cafleet/src/cafleet/webui/api.py returns a JSON dict with default status 200 (list_fleets, list_agents, get_monitor, get/patch agent monitor, inbox, sent, timeline, send_message) — grep for `204` across cafleet/src/cafleet/webui/api.py, app.py, and admin/src finds only this client-side branch.
- **Proposed change**: Delete the three-line `resp.status === 204` branch from `request<T>()`.
- **Why behavior-preserving**: The branch is unreachable: no backend route returns 204, so `resp.json()` is always the path taken today. Removing unreachable code cannot change any observable behavior.
- **Verifier notes**: Verified. admin/src/api.ts:38-40 is the only occurrence of '204' anywhere in the repo (source, docs/, SPEC.md, README.md, skills/). All 9 routes in cafleet/src/cafleet/webui/api.py return plain dicts (FastAPI default 200) or raise HTTPException 4xx, which the `!resp.ok` branch at api.ts:33 intercepts before line 38; webui/app.py adds no middleware or other routers, and the SPA fallback never serves /api paths with 204. No frontend tests exist (admin/package.json has no test script) and no doc documents 204 handling as a contract. The branch is unreachable; deleting it preserves all observable behavior.

### [admin-ui-2] Unused response-type fields: TimelineMessage.type and five MonitorRuntime process fields
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `admin/src/types.ts:10-13, 32`
- **Estimated impact**: -6 lines, smaller type surface to keep in sync
- **Evidence**: types.ts:32 declares `type: string` on TimelineMessage; grep for `.type`, `row.type`, `{ type`, `"type"` across admin/src finds zero reads. types.ts:9-13 declare `pid`, `tick_seconds`, `last_tick_at`, `last_tick_age_seconds`, `started_at` on MonitorRuntime; grep shows their only occurrence in admin/src is the declaration itself — the sole consumer is Dashboard.tsx:100 (`monitor.running`). The backend (webui/api.py `_format_messages` and `_monitor_runtime_payload`) does return these keys, but the UI never reads them.
- **Proposed change**: Drop `type` from `TimelineMessage` and the five unused fields from `MonitorRuntime` (or narrow `getMonitor`'s return to `{ running: boolean }`-shaped type). TS interfaces are structural, so extra JSON keys in the response remain valid.
- **Why behavior-preserving**: TypeScript types are erased at build time; the emitted bundle is byte-identical. The HTTP API and rendered UI are untouched — the fields keep arriving in JSON and are simply no longer declared.
- **Verifier notes**: Verified. Repo-wide grep confirms TimelineMessage.type (types.ts:32) and pid/tick_seconds/last_tick_at/last_tick_age_seconds/started_at (types.ts:9-13 — note pid is line 9, not 10) are declaration-only in admin/src; the sole MonitorRuntime consumer is Dashboard.tsx:100 (monitor.running), with type-position uses at Dashboard.tsx:29 and api.ts:80 that the change would update. STATUS_CHIPS uses TimelineMessage["status"] only. TS interfaces are structural and erased at build; the server payloads (SPEC.md §6.8, webui/api.py _format_messages / _monitor_runtime_payload) are untouched, so no documented contract changes. No admin tests or docs reference the client-side type fields.

### [admin-ui-3] Duplicated refreshKey-driven in-flight-guarded fetch pattern in Timeline and AgentDetail
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `admin/src/components/Timeline.tsx:101-124 (and AgentDetail.tsx:231-264)`
- **Estimated impact**: ~-15 net lines across 2 files; one pattern instead of three hand-rolled copies
- **Evidence**: Timeline.tsx:101-124 and AgentDetail.tsx:231-264 implement the identical structure: `const inFlightRef = useRef(false)` + `useCallback(async () => { if (inFlightRef.current) return; inFlightRef.current = true; try { ...fetch+set... } catch { /* swallow */ } finally { setLoading(false); inFlightRef.current = false; } })` + `useEffect(() => { void load(); }, [refreshKey, load])`. AgentDetail.tsx:239-241 even says 'Mirrors Timeline's pattern'. The same guarded-trigger logic exists a third time in hooks/usePolling.ts:25-35.
- **Proposed change**: Extract a small hook, e.g. `useRefreshKeyLoad(load, refreshKey)` (in hooks/), that owns the in-flight ref and the refreshKey effect; Timeline and AgentDetail keep only their fetch+setState bodies. Optionally share the guarded-trigger core with usePolling.
- **Why behavior-preserving**: The hook encapsulates the exact same guard, swallow-on-error, and effect trigger already present at both call sites; call order, fetches issued, and state transitions (including setLoading(false) in finally) are unchanged.
- **Verifier notes**: Verified. Timeline.tsx:101-124 and AgentDetail.tsx:231-264 implement the identical guard+swallow+finally+useEffect([refreshKey, load]) structure (AgentDetail.tsx:239 literally says 'Mirrors Timeline's pattern'), and usePolling.ts:25-35 is a third structural copy. Extraction is behavior-preserving PROVIDED: (a) the hook keeps `load` in its effect deps — AgentDetail's load is recreated on agent.agent_id change and that dep is what triggers the agent-switch reload; (b) setLoading(false) stays in each component's own finally so it fires only on executed (non-guarded-out) loads; (c) the shared ref stays per-component-instance. Folding usePolling's trigger into the same core is also feasible (identical guard/swallow semantics) but is correctly marked optional.

### [admin-ui-4] App.tsx refetches listFleets + getAgents after handleSelectFleet and discards the results
- **Status**: REFUTED/RESCOPED
- **Kind**: simplifiable-logic
- **Location**: `admin/src/App.tsx:40-90, 92-106`
- **Estimated impact**: Removes 2 redundant HTTP round-trips per fleet selection; ~+3/-0 lines but one less implicit double-fetch path to reason about
- **Evidence**: handleSelectFleet (App.tsx:92-106) already calls setFleetId, getAgents, setAgents, setFleetLabel, sets loadedFleetIdRef, then navigates. The navigation re-runs the route effect (App.tsx:40-90), which — because `loadedFleetIdRef.current === route.fleetId` — skips only the skeleton reset (lines 51-55) but still fetches listFleets AND getAgents again (lines 61, 72). Those second results are effectively discarded: Dashboard is already mounted at that point and consumes `initialAgents` only as a useState initializer (Dashboard.tsx:28), and setFleetLabel re-sets the same label handleSelectFleet just set. This ref===fleetId case is reachable only immediately after handleSelectFleet (handleBack and the non-dashboard branch clear the ref at lines 43 and 109-111).
- **Proposed change**: In the route effect, early-return when `loadedFleetIdRef.current === route.fleetId` (data for this fleet is already loaded and Dashboard is mounted), keeping the full fetch path for deep links and cross-fleet hash changes.
- **Why behavior-preserving**: The skipped fetches feed state that the mounted Dashboard ignores (useState initializer) or that is set to an identical value; `loading` is already false on this path so no render branch changes. Rendered UI is pixel-identical; the only observable difference is two fewer redundant HTTP requests per fleet selection, and Dashboard's own 5s poll (Dashboard.tsx:52) is the actual freshness mechanism.
- **Verifier notes**: The double-fetch mechanics are accurately described, but the change is NOT strictly behavior-preserving and the claim 'the only observable difference is two fewer redundant HTTP requests' is false. The refetch path the early-return would skip also carries: (1) fleet re-validation — if listFleets/getAgents fails transiently or the fleet was concurrently soft-deleted (broker/fleets.py delete_fleet exists, and the X-Fleet-Id dependency in webui/api.py does not reject soft-deleted fleets, so this listFleets re-check is the only mechanism that bounces the user out), the current code navigates back to '/fleets'; the proposal silently keeps the user on the dashboard — an observable UI behavior change, undisclosed; (2) setFleetLabel(fleet.label) re-set from the server (same value in practice since labels have no update path, but still a dropped read-back). Evidence error: handleBack (App.tsx:108-112) does not clear loadedFleetIdRef — only the non-dashboard effect branch at line 42 does. The happy-path analysis (Dashboard consumes initialAgents only as a useState initializer, loading stays false) is correct, so a weaker finding ('redundant in the success path') would be valid, but as stated — 'pixel-identical, only difference is fewer requests' — it is refuted.

### [admin-ui-5] Duplicated zero-padded HH:MM formatting helpers
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `admin/src/components/AgentDetail.tsx:32-36 (and TimelineMessage.tsx:39-43)`
- **Estimated impact**: ~-8 lines, removes drift risk between the two time formats
- **Evidence**: AgentDetail.tsx:32-36 `formatDateTime` and TimelineMessage.tsx:39-43 `formatTime` both define `const pad = (n: number) => String(n).padStart(2, "0")` and build `${pad(d.getHours())}:${pad(d.getMinutes())}`; formatDateTime only appends ` · ${d.toLocaleDateString()}`.
- **Proposed change**: Move a shared `formatTime(iso)` (and `formatDateTime(iso)` composed from it) into one module (e.g. a small `format.ts` next to timeline.ts, or export from one component) and import at both call sites.
- **Why behavior-preserving**: Pure string-formatting extraction producing byte-identical output for all inputs; no rendering or logic change.
- **Verifier notes**: Verified. AgentDetail.tsx:32-36 formatDateTime and TimelineMessage.tsx:39-43 formatTime both define the identical pad helper and `${pad(getHours())}:${pad(getMinutes())}` core; formatDateTime only appends ` · ${d.toLocaleDateString()}`. formatDateTime has two call sites inside AgentDetail (lines 79 and 207), formatTime one (TimelineMessage.tsx:70) — all stay behavior-identical under a shared-module extraction producing byte-identical output. No tests, docs, or SPEC sections reference these client-side helpers or their module locations.

## Area: broker-db (3/8 confirmed)

### [broker-db-1] is_monitoring_member() has zero production callers — monitoring-member detection happens in SQL
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/broker/_shared.py:59-60`
- **Estimated impact**: -3 lines in _shared.py, roughly -20 lines of tests that exercise dead code
- **Evidence**: Definition at _shared.py:59-60 (`def is_monitoring_member(agent_card_json: str | None) -> bool`). A repo-wide grep shows no call site in cafleet/src/ — production identifies the monitoring member via `func.json_extract(Agent.agent_card_json, "$.cafleet.kind") == _shared.MONITORING_MEMBER_KIND` in SQL (agents.py:95-96, monitor.py:85-86). The only usages are the helper's own unit tests: tests/broker/test_kind_predicate_safety.py:16,41,44-46 and tests/broker/test_monitor.py:140-150. The sibling `is_administrator` IS used in production (agents.py:119, 282), so only the monitoring variant is dead.
- **Proposed change**: Delete `is_monitoring_member` from _shared.py and prune its dedicated tests: reduce `_PREDICATES` in test_kind_predicate_safety.py to just `is_administrator` (the malformed-card guard lives in the shared `_card_kind`, so coverage is preserved via `is_administrator`), and drop test_monitor.py's `test_is_monitoring_member__kind_detection` parametrized test.
- **Why behavior-preserving**: The function is never executed by any production code path; removal changes no observable CLI/HTTP/DB behavior. The shared `_card_kind` guard remains fully exercised through `is_administrator` tests.
- **Verifier notes**: Code-level claim verified: zero production call sites in cafleet/src (SQL json_extract does the detection at agents.py:95-96 and monitor.py:85-86; only tests call the helper). BUT the scan missed SPEC.md, which documents is_monitoring_member as part of the reimplementation contract in three places: SPEC.md:371-373 (Internal predicates), SPEC.md:515-518 (Kind predicates section specifying its malformed-JSON non-match semantics), and SPEC.md:2377-2378 (fail-fast points catalog). Per this repo's rules SPEC.md is a first-class contract surface and SPEC drift is a blocker; the removal rule requires deleting every mention in the same change. As scoped (delete function + prune tests, no SPEC edits) the change leaves SPEC specifying a function that no longer exists — not strictly behavior-preserving with respect to documented contracts. Valid only if rescoped to include the three SPEC.md edits.

### [broker-db-2] broker.list_agents() is unused by production — CLI and WebUI use list_roster/list_fleet_agents instead
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/broker/agents.py:229-251`
- **Estimated impact**: -25 lines in agents.py plus 2 export lines; one fewer redundant query function to keep in sync
- **Evidence**: Definition at agents.py:229-251; exported at broker/__init__.py:8,52. No `cafleet agent list` CLI command exists (CLI groups: fleet, member, message, monitor, setup, doctor), the WebUI route named list_agents (webui/api.py:120) calls `broker.list_fleet_agents(fleet_id)` (api.py:121), and `cafleet member list` uses `broker.list_roster` / `broker.list_members_with_activity` / `broker.list_members` (cli/member.py:511-515). Only tests call it: tests/broker/test_registry.py:302,315,324,365; test_administrator.py:118,143,172; tests/cli/test_member.py:598.
- **Proposed change**: Delete `list_agents` from agents.py and remove its import/__all__ entries in broker/__init__.py; migrate the 8 test call sites to `broker.list_roster(fleet_id)` (same active-agents-of-fleet population, superset row shape) or assert against `list_fleet_agents`.
- **Why behavior-preserving**: No production code path reaches the function; the broker package is an internal Python API, not a documented contract surface (SPEC.md covers CLI/config/schema/HTTP). Tests keep asserting the same registry facts through the live functions.
- **Verifier notes**: The no-production-caller evidence checks out (webui/api.py:120-121 route calls list_fleet_agents; cli/member.py:511 uses list_roster; only 8 test call sites), and list_roster does cover the same population (active agents of the fleet) with a superset row shape. BUT the finding's core rationale is factually wrong: 'the broker package is an internal Python API, not a documented contract surface' is contradicted by SPEC.md §6.2, which documents the broker layer function-by-function — list_agents(fleet_id) with its exact row shape at SPEC.md:598, its kind-surfacing behavior at SPEC.md:367 and :520, and its soft-delete visibility at SPEC.md:789. Additionally docs/api/broker.md documents a 'Re-export contract' (broker/__init__.py re-exports every submodule's public API via __all__) and renders the package with mkdocstrings (::: cafleet.broker), so deleting list_agents also changes the rendered API docs. Removal without SPEC.md + docs/api/broker.md edits violates documented contracts; not behavior-preserving as scoped.

### [broker-db-3] record_ping() thin wrapper is tests-only; production uses record_pings
- **Status**: REFUTED/RESCOPED
- **Kind**: redundant-abstraction
- **Location**: `cafleet/src/cafleet/broker/monitor.py:201-203`
- **Estimated impact**: -5 lines plus 2 export lines
- **Evidence**: monitor.py:201-203 — `def record_ping(agent_id, when): record_pings([agent_id], when)`, exported at broker/__init__.py:38,75. The only production caller of ping recording is the loop: monitor/loop.py:106 `broker.record_pings([...])`. `record_ping` appears only in tests: tests/monitor/test_loop.py:138,203; tests/broker/test_monitor.py:332; tests/cli/test_monitor.py:361,379.
- **Proposed change**: Delete `record_ping` and its two __init__.py lines; change the 5 test call sites to `broker.record_pings([agent_id], when)`.
- **Why behavior-preserving**: The wrapper is a pure delegation to `record_pings` with a one-element list — the exact call the tests would make directly. No production path invokes it.
- **Verifier notes**: Verified: the only production ping-recording caller is monitor/loop.py's broker.record_pings, and the 5 cited test call sites are accurate. BUT 'appears only in tests' is false: SPEC.md:737-738 explicitly specifies record_ping ('**record_ping** is a thin wrapper over record_pings([agent_id], when)') as part of the Monitor schedule-CRUD contract, and docs/api/broker.md:22 lists record_ping among broker/monitor.py's public API — which the documented Re-export contract plus the ::: cafleet.broker mkdocstrings directive render as user-facing API docs (it is in __all__ at broker/__init__.py:75). Deleting it as scoped (code + __init__ + test edits only) creates SPEC and docs drift on documented surfaces. Valid only if rescoped to include the SPEC.md:737-738 and docs/api/broker.md:22 edits.

### [broker-db-4] enroll_agent is exported from broker/__init__.py but has no callers outside the broker package
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/broker/__init__.py:30, 70`
- **Estimated impact**: -2 lines; broker facade no longer exposes a session-taking internal helper
- **Evidence**: `enroll_agent` is imported at __init__.py:30 and listed in __all__ at line 70, but grep over cafleet/src (cli, webui, monitor) and cafleet/tests finds zero `broker.enroll_agent` / `enroll_agent` references outside the broker package itself. Its only callers are internal: agents.py:161 and fleets.py:84 (both `monitor.enroll_agent(session, ...)`). It is also the only export taking a live `session` argument — an intra-transaction helper, not part of the facade shape the rest of __all__ presents.
- **Proposed change**: Remove `enroll_agent` from the import block and __all__ in broker/__init__.py; keep the function itself in monitor.py where agents.py/fleets.py call it via the module.
- **Why behavior-preserving**: Only the public re-export is removed; both internal call sites import `monitor` directly (`from cafleet.broker import _shared, monitor`), so all registration/enrollment behavior is untouched.
- **Verifier notes**: Runtime facts verified: no src or test code references broker.enroll_agent through the facade; the only callers are monitor.enroll_agent at agents.py:161 and fleets.py:84 via direct module import, and it is indeed the only __all__ entry taking a live session (delete_fleet_monitor_rows / delete_agent_monitor_row are NOT exported). BUT docs/api/broker.md § 'Re-export contract' documents that broker/__init__.py re-exports the public API of every submodule except skill_installs.py, and line 22 of that page lists enroll_agent first among monitor.py's public API; the ::: cafleet.broker mkdocstrings directive renders __all__, so dropping the export removes enroll_agent from the rendered API page and makes the documented re-export contract false. SPEC.md:505 and :719 also document enroll_agent as a broker function (those stay accurate since the function survives). The change alters a documented contract without the paired docs edit — not strictly behavior-preserving as scoped.

### [broker-db-5] The 4-column monitor_config SELECT is written out three times in monitor.py
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/broker/monitor.py:102-111, 119-127, 172-179`
- **Estimated impact**: -10 lines; one home for the config row shape alongside _config_dict
- **Evidence**: The identical column list `select(MonitorConfig.agent_id, MonitorConfig.interval_seconds, MonitorConfig.last_ping_at, MonitorConfig.enabled)` appears verbatim in `get_monitor_config` (monitor.py:102-111), `list_monitor_configs` (119-127), and the post-update re-read inside `update_monitor_config` (172-179); all three feed the same `_config_dict` mapper (43-49).
- **Proposed change**: Introduce a module-level tuple `_CONFIG_COLS = (MonitorConfig.agent_id, MonitorConfig.interval_seconds, MonitorConfig.last_ping_at, MonitorConfig.enabled)` and build each statement as `select(*_CONFIG_COLS)` with its own WHERE/JOIN.
- **Why behavior-preserving**: Same columns, same order, same statements are emitted to SQLite; only the Python construction is deduplicated.
- **Verifier notes**: Verified: the identical select(MonitorConfig.agent_id, interval_seconds, last_ping_at, enabled) column list appears in get_monitor_config (monitor.py:102-111), list_monitor_configs (monitor.py:119-128), and the post-update re-read in update_monitor_config (monitor.py:172-179), all mapped through _config_dict (monitor.py:43-49). A module-level _CONFIG_COLS tuple with select(*_CONFIG_COLS) plus each site's own WHERE/JOIN emits identical statements and result sets; SQLAlchemy column objects are safely shared at module level. SPEC.md §6.2 documents only the returned dict shapes (lines 726-735), not the Python statement construction, and no test or doc pins the construction. Behavior-preserving as claimed.

### [broker-db-6] The $.cafleet.kind json_extract expression is rebuilt at five sites across four broker modules
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/broker/agents.py:95-96, 372-374`
- **Estimated impact**: -8 lines; single home for the card-kind JSON path used by 4 modules
- **Evidence**: The SQL kind-extraction expression is duplicated: agents.py:95-96 (`func.json_extract(Agent.agent_card_json, "$.cafleet.kind") == _shared.MONITORING_MEMBER_KIND`), agents.py:372-374 (`func.coalesce(func.json_extract(...), "")`), members.py:86-88 (identical coalesce), messaging.py:206-210 (coalesce compared `!= ADMINISTRATOR_KIND`), monitor.py:85-86 (raw json_extract compared to MONITORING_MEMBER_KIND). The JSON path string "$.cafleet.kind" is hard-coded five times.
- **Proposed change**: Add one _shared.py module-level expression, e.g. `CARD_KIND_SQL = func.coalesce(func.json_extract(Agent.agent_card_json, "$.cafleet.kind"), "")`, and use it at all five sites (comparisons against the kind constants stay at each site).
- **Why behavior-preserving**: For the two non-coalesced sites (agents.py:95, monitor.py:85) the comparison is against a non-empty constant, so `NULL == kind` (NULL, non-match) and `"" == kind` (false, non-match) select exactly the same rows; the three coalesced sites are byte-identical reuse. Emitted result sets are unchanged.
- **Verifier notes**: All five sites verified at the cited lines: agents.py:95-96 (raw json_extract == MONITORING_MEMBER_KIND), agents.py:372-374 (coalesce), members.py:86-88 (coalesce), messaging.py:206-210 (coalesce != ADMINISTRATOR_KIND), monitor.py:85-86 (raw == MONITORING_MEMBER_KIND). The NULL-vs-empty-string analysis is correct: against a non-empty constant, NULL == k yields NULL (non-match) and '' == k yields false (non-match), so wrapping the two raw sites in coalesce(...,'') selects identical rows; the three coalesce sites are byte-identical reuse. The remaining copies of the path live only in frozen alembic migration 0005 (raw SQL), correctly out of scope. All four modules already import _shared, and _shared already imports Agent — no cycle. docs/spec/data-model.md:147 describes find_monitoring_member's predicate as json_extract == 'monitoring-member', which remains semantically accurate after the coalesce wrap. Confirmed behavior-preserving; only the emitted SQL text (not results) changes at two sites.

### [broker-db-7] heartbeat_monitor_runtime redundantly writes pid=pid in a WHERE pid == pid update
- **Status**: CONFIRMED
- **Kind**: simplifiable-logic
- **Location**: `cafleet/src/cafleet/broker/monitor.py:329-335`
- **Estimated impact**: -1 token of misleading code; the ownership-check intent reads cleanly
- **Evidence**: monitor.py:330-334: `update(MonitorRuntime).where(MonitorRuntime.fleet_id == fleet_id, MonitorRuntime.pid == pid).values(pid=pid, last_tick_at=when)`. Any row matched by the WHERE clause already has `pid == pid`, so the `pid=pid` assignment is a no-op on every matched row.
- **Proposed change**: Drop `pid=pid` from the .values() call, leaving `.values(last_tick_at=when)`.
- **Why behavior-preserving**: Rows matched by the WHERE already hold that pid value, so the resulting row contents and the `rowcount == 1` ownership signal are identical.
- **Verifier notes**: Verified at monitor.py:329-335: the WHERE clause includes MonitorRuntime.pid == pid, so every matched row already holds that pid and values(pid=pid) is a no-op. Dropping it leaves the matched-row set unchanged, and sqlite3's changes()-based rowcount counts the same rows (last_tick_at is still written), so the rowcount == 1 ownership signal and the tests at tests/broker/test_monitor.py:488-517 are unaffected. One scope correction: SPEC.md:761-762 currently specifies the heartbeat as 'update pid and last_tick_at = when only where the current pid equals the caller's pid' — that sentence must get the matching one-line edit in the same change (per the repo's SPEC-drift-is-a-blocker rule), though observable DB state is identical either way.

### [broker-db-8] Sync SQLite URL derivation duplicated between db/engine.py and db/init.py
- **Status**: REFUTED/RESCOPED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/db/init.py:17-18`
- **Estimated impact**: -3 lines; one home for the aiosqlite-to-sqlite driver swap that must stay consistent
- **Evidence**: init.py:17-18 defines `_sync_db_url()` returning `str(make_url(settings.database_url).set(drivername="sqlite"))`; engine.py:35 inlines the exact same expression (`sync_url = str(make_url(settings.database_url).set(drivername="sqlite"))`). (A third variant in db/alembic/env.py:20-22 takes the alembic config option first, so it legitimately differs.)
- **Proposed change**: Move the helper to engine.py as a small public `sync_db_url()` and call it from both `_get_sync_engine()` and `run_db_init()`, deleting init.py's private copy.
- **Why behavior-preserving**: Both sites compute the identical string from the same `settings.database_url`; centralizing the expression changes no URL, connection, or migration behavior.
- **Verifier notes**: The duplication facts are accurate: init.py:17-18 and engine.py:35 compute the identical expression, and db/alembic/env.py:20-22 legitimately differs (config option first). But the specific proposed change — moving the helper into engine.py and importing it from run_db_init — is not strictly behavior-preserving: cafleet.db.init currently does NOT import cafleet.db.engine, and importing engine.py has a documented module-level side effect (engine.py:1-7, 22-29): it registers a global Engine 'connect' listener applying PRAGMA foreign_keys=ON and busy_timeout=5000 to every subsequent SQLite connection in the process. In the packaged CLI the listener happens to be pre-registered (cli/setup.py imports broker.skill_installs → broker._shared → db.engine), but any process importing only cafleet.db.init (isolated runs of tests/db/test_init.py, direct library use) would newly run Alembic's migration connections — including the batch table-recreates in migrations 0001 and 0007 — with FK enforcement and busy_timeout enabled, an environmental change the finding does not analyze. The dedup is sound only with a side-effect-free home for the helper (or engine.py importing it), not as proposed.

## Area: cli (8/8 confirmed)

### [cli-1] client_command carries two production-dead branches: the non-truncating output path and the formatter-less JSON fallback
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/cli/_helpers.py:93-151`
- **Estimated impact**: -15 to -20 lines in _helpers.py, one fewer decorator mode to reason about, simpler tests
- **Evidence**: All six production call sites (cafleet/src/cafleet/cli/message.py:36, 69, 93, 112, 132, 150) pass truncates_task_text=True AND a text_formatter. grep confirms client_command is used nowhere else in src/. Therefore the else-branch at _helpers.py:131-132 (`rendered = result`), the non-truncating formatter call at :139-140 (`text_formatter(result)`), and the text-mode JSON fallback at :141-142 (`else: click.echo(output.format_json(rendered))` when text_formatter is None) are reachable only from the synthetic commands in tests/cli/test_client_command.py:35, 55 — which exist to exercise those very branches. Additionally `full = kwargs.get("full", False)` at :127 never uses its default because all six commands declare @full_flag.
- **Proposed change**: Drop the truncates_task_text parameter (behavior always on), make text_formatter required, delete the three dead branches and the two-mode docstring (lines 99-107), and replace kwargs.get("full", False) with kwargs["full"] per the project's no-meaningless-fallbacks rule. Update the test-only commands in tests/cli/test_client_command.py to the single remaining mode.
- **Why behavior-preserving**: Every production command already takes the truncating path with a formatter; the removed branches have no production call site, so byte-identical output, exit codes, and JSON for all `cafleet message` subcommands.
- **Verifier notes**: Verified: all six production call sites (cafleet/src/cafleet/cli/message.py:36,69,93,112,132,150) pass truncates_task_text=True and a text_formatter; repo-wide grep finds no other production caller of client_command. All six commands declare @full_flag, so kwargs.get('full', False) at _helpers.py:127 never uses its default. Two scope corrections: (1) SPEC.md:908-926 ('The client_command wrapper (message group)') documents the optional text renderer, the truncate/pass-through render branch, and the 'else emit JSON' fallback as part of the reimplementation spec — per the project's documentation-maintenance rules, that SPEC section MUST be updated in the same change, otherwise this is SPEC drift; the proposed_change omits this. (2) Evidence detail: test_client_command.py's `_raises` command (line 55, client_command() with no formatter) always raises before output, so the formatter-less JSON fallback at _helpers.py:141-142 is not exercised by any test at all — it is fully dead, which strengthens the finding; the test still needs updating since text_formatter becomes required. With the SPEC edit included, observable CLI behavior (output bytes, exit codes, JSON) is preserved.

### [cli-2] Six byte-identical --agent-id option declarations in message.py that _helpers could absorb
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/message.py:24, 59, 90, 107, 128, 146`
- **Estimated impact**: -5 duplicated lines, single source of truth for the shared option
- **Evidence**: The exact line `@click.option("--agent-id", type=int, required=True, help="Agent ID")` appears six times (message send/broadcast/poll/ack/cancel/show), verified by grep. _helpers.py already hosts the identical pattern for full_flag (:47-53), quiet_flag (:54-60), and director_member_options (:63-66), so this is the one shared option message.py still declares inline.
- **Proposed change**: Add `agent_id_option = click.option("--agent-id", type=int, required=True, help="Agent ID")` to _helpers.py next to full_flag/quiet_flag and use it at all six sites. (member.py's --agent-id declarations have different help texts and stay as-is.)
- **Why behavior-preserving**: A module-level click.option decorator constant produces the identical Option object; flag name, type, required-ness, and help string are byte-identical, so --help output and parsing are unchanged.
- **Verifier notes**: Verified by grep: the exact line `@click.option("--agent-id", type=int, required=True, help="Agent ID")` appears at message.py:24,59,90,107,128,146 and nowhere else; member.py:156 (help="Director's agent ID"), member.py:684-688 (multi-line, different help), and monitor.py:133 (name-mapped, help="Target agent.") all differ and are correctly excluded. Reusing a module-level click.option decorator constant is safe — click constructs a fresh Option inside the decorator per application, and the repo already proves the pattern with full_flag/quiet_flag/fleet_id_option applied across many commands. Applying the constant at the same decorator position preserves --help parameter ordering. Behavior-preserving.

### [cli-3] --text/--text-file option pair declared four times with identical --text-file help
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/message.py:26-32, 60-66`
- **Estimated impact**: -20 lines of decorator boilerplate across message.py and member.py
- **Evidence**: The two-option block is declared at message.py:26-32 (send), message.py:60-66 (broadcast), member.py:182-189 (create), member.py:690-696 (nudge). The --text-file declaration is byte-identical in all four (`help="File (UTF-8) or '-' for stdin."`); only the --text help varies ("Message body (inline)." x2, "Inline spawn prompt.", "Re-engage summary (inline)."). _text_input.py:3-5 already documents these as "the four text-body commands" sharing one reader, so the option surface is one coherent unit split across four sites.
- **Proposed change**: Add a parametrized decorator to _helpers (or _text_input), e.g. `def text_body_options(text_help: str)` that applies both click.options, passing the per-command --text help string through verbatim; use at the four call sites.
- **Why behavior-preserving**: The decorator emits the same click.option calls with the same names, defaults, and help strings passed as literals, so --help text and parsing stay byte-identical.
- **Verifier notes**: The four sites are real (message.py:26-32 send, :60-66 broadcast; member.py:182-189 create, :690-696 nudge) and _text_input.py:3-7 documents them as the four text-body commands. One evidence correction: the --text-file declaration is NOT byte-identical in all four — member create's pair (member.py:182-188) includes `type=str,` which the other three omit (only the help string is identical everywhere). This does not change the verdict: click infers STRING for default=None, so with or without type=str the Option type, metavar (TEXT), parsing, and --help output are identical, and a shared text_body_options(text_help) decorator emitting a single normalized form is behavior-preserving. Implementation must apply --text before --text-file (matching every current site's declaration order) to keep --help ordering byte-identical.

### [cli-4] Duplicated urllib error-mapping blocks in setup's release lookup and asset download
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/setup.py:61-76, 97-108`
- **Estimated impact**: -12 lines, one error-mapping to maintain instead of two
- **Evidence**: _resolve_download_url (:61-76) and _download_and_extract (:97-108) contain near-identical try/except ladders: both map urllib.error.HTTPError to `could not reach the GitHub API ({exc.reason})` and (URLError, TimeoutError) to the same message via `reason = getattr(exc, "reason", exc)`. The only delta is the 404 special case at :65-68 in the resolver.
- **Proposed change**: Factor a private `_http_get(req) -> bytes` helper owning the urlopen + the shared except ladder; _resolve_download_url wraps it to intercept the HTTPError-404 case (e.g. via a small on_404 message parameter or a re-raise catch), _download_and_extract calls it directly.
- **Why behavior-preserving**: Both call sites raise ClickException with the exact same message strings today; the helper reproduces those strings and the 404 path verbatim, so error text and exit codes are unchanged.
- **Verifier notes**: Verified: setup.py:61-76 (_resolve_download_url) and :97-108 (_download_and_extract) contain the same HTTPError -> 'could not reach the GitHub API ({exc.reason})' and (URLError, TimeoutError) -> same message via getattr(exc, 'reason', exc) ladders; the only delta is the 404 special case at :65-68. A shared _http_get(req) -> bytes helper preserves behavior including the except ordering (HTTPError before its URLError parent). One implementation caveat: in _download_and_extract the archive_path.write_bytes(resp.read()) currently sits inside the try, but a plain OSError from write_bytes is not a URLError and is not caught today, so moving the write outside the helper (helper returns bytes, caller writes) is behavior-identical. The 404 interception must live only in the resolver wrapper; the download path today maps a 404 HTTPError to the generic message, and the helper must keep that.

### [cli-5] setup skill --agent Choice list restates AGENT_SKILLS_DIRS keys
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/setup.py:203-210`
- **Estimated impact**: -1 duplicated literal; adding a backend later touches one place instead of two
- **Evidence**: setup.py:206 hardcodes `type=click.Choice(["claude", "codex", "opencode"])` while AGENT_SKILLS_DIRS at :24-28 defines exactly those three keys in the same order, and _install_skills indexes AGENT_SKILLS_DIRS[agent] with the chosen value (:143). fleet.py:22 already uses the analogous pattern `click.Choice(list(CODING_AGENTS.keys()))` instead of a literal list.
- **Proposed change**: Replace the literal list with `click.Choice(list(AGENT_SKILLS_DIRS))` so the option choices derive from the single dict that the install path consumes.
- **Why behavior-preserving**: dict key order matches the literal list exactly (claude, codex, opencode), so the rendered metavar in --help, accepted values, and error messages for invalid choices are byte-identical.
- **Verifier notes**: Verified: setup.py:206 hardcodes click.Choice(["claude", "codex", "opencode"]) and AGENT_SKILLS_DIRS (setup.py:24-28) defines exactly those keys in the same insertion order; _install_skills indexes AGENT_SKILLS_DIRS[agent] at :143; fleet.py:22 already uses the derived-Choice pattern with CODING_AGENTS. click.Choice(list(AGENT_SKILLS_DIRS)) yields the identical choices tuple, so --help metavar, accepted values, and invalid-choice errors are byte-identical. Test interaction checked: tests/cli/test_setup.py:152 monkeypatches AGENT_SKILLS_DIRS after import, but the Choice is evaluated at decoration (import) time in both the current and proposed forms, and the test mapping uses the same three keys anyway — no behavioral difference.

### [cli-6] member delete timeout path re-implements _emit_member_delete_output's JSON branch inline
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/member.py:419-424`
- **Estimated impact**: -6 lines, one JSON emitter for all four member-delete outcomes
- **Evidence**: member.py:419-424 (`if ctx.obj["json_output"]: click.echo(output.format_json({"agent_id": member_id, "pane_status": pane_status}))`) is character-for-character the JSON branch of _emit_member_delete_output at :450-455. The timeout path bypasses the helper only because it must print no stdout header in text mode.
- **Proposed change**: Give _emit_member_delete_output an optional `header: str | None` where None means JSON-only (skip the text branch entirely), and have the timeout path call `_emit_member_delete_output(ctx, member_id, pane_status, header=None)` before ctx.exit(2).
- **Why behavior-preserving**: JSON payload keys, values, and formatting are identical; in text mode the timeout path prints nothing to stdout both before and after; ctx.exit(2) stays at the call site so the exit code is unchanged.
- **Verifier notes**: Verified: member.py:419-424 is character-for-character the JSON branch of _emit_member_delete_output (member.py:450-455), including the trailing-comma dict formatting. The helper has exactly three current call sites (member.py:345, 363, 390), all with headers, and the timeout path is the fourth outcome; in text mode the timeout path prints only to stderr (all click.echo(..., err=True)), so a header=None mode that skips the text branch prints nothing to stdout, matching today. ctx.exit(2) remains at the call site. JSON payload keys/values identical. Behavior-preserving.

### [cli-7] ensure_skills_current raises the same error message from two separate branches
- **Status**: CONFIRMED
- **Kind**: simplifiable-logic
- **Location**: `cafleet/src/cafleet/cli/_helpers.py:24-34`
- **Estimated impact**: -4 lines, one copy of the error string
- **Evidence**: Lines 26-29 and 30-34 raise ClickException with the byte-identical string "no skills install is recorded; run 'cafleet setup' first" — once for a missing table, once for an empty table.
- **Proposed change**: Collapse to a single short-circuited check, e.g. `rows = list_skill_installs() if skill_installs_table_exists() else []` followed by one `if not rows: raise click.ClickException(...)`, keeping the table-exists guard so list_skill_installs is never called against a missing table.
- **Why behavior-preserving**: Both branches raise the same exception type with the same message; the short-circuit preserves the invariant that list_skill_installs only runs when the table exists, so all inputs produce identical output and exit code.
- **Verifier notes**: Verified: _helpers.py:26-29 (missing table) and :30-34 (empty rows) raise click.ClickException with the byte-identical string "no skills install is recorded; run 'cafleet setup' first". The proposed short-circuit (rows = list_skill_installs() if skill_installs_table_exists() else []; if not rows: raise) preserves the invariant that list_skill_installs never runs against a missing table, and both input states still produce the same exception type, message, and exit code 1. SPEC.md documents only the error string, which is unchanged. Behavior-preserving.

### [cli-8] fleet.py declares the per-subcommand --json flag and the as_json-or-ctx check three times
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/fleet.py:27, 59, 84`
- **Estimated impact**: -8 lines of repeated declaration/check boilerplate
- **Evidence**: `@click.option("--json", "as_json", is_flag=True, help="Output as JSON.")` is byte-identical at fleet.py:27, :59, :84 (create/list/show), and the pairing check `if as_json or ctx.obj["json_output"]:` repeats at :52, :65, :93. No other CLI module declares a subcommand-local --json, so this duplication is contained within one file.
- **Proposed change**: Hoist a module-level `_json_flag = click.option("--json", "as_json", is_flag=True, help="Output as JSON.")` (or place it in _helpers next to full_flag) and apply it at the three sites; optionally a one-line `_wants_json(ctx, as_json)` helper for the repeated check.
- **Why behavior-preserving**: A shared click.option constant produces the identical option (name, dest, flag semantics, help), and the extracted predicate computes the same boolean, so parsing, --help output, and the JSON/text branching are unchanged.
- **Verifier notes**: Verified: the option line `@click.option("--json", "as_json", is_flag=True, help="Output as JSON.")` is byte-identical at fleet.py:27, :59, :84, and the check `if as_json or ctx.obj["json_output"]:` repeats at :52, :65, :93. Grep confirms no other CLI module declares a subcommand-local --json (cli/__init__.py:17 is the distinct global --json/json_output flag with different help and dest, correctly out of scope). A hoisted decorator constant produces an identical Option per application (same pattern as full_flag), and a _wants_json(ctx, as_json) predicate computes the same boolean. Parsing, --help output, and branching unchanged. Behavior-preserving.

## Area: docs (5/7 confirmed)

### [docs-1] How-to guides restate the Monitoring concepts page in full instead of linking
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `docs/how-to/monitor-and-recover.md:11-45 (and docs/how-to/mixed-backend-team.md:33-41)`
- **Estimated impact**: -25 to -30 lines across two how-to pages; removes two drift-prone copies of the monitoring contract
- **Evidence**: docs/how-to/monitor-and-recover.md:11-45 (§ 'Ensure the monitor is running') re-explains the whole monitoring model already canonical on docs/concepts/monitoring.md: the watched set with the 180 s / 720 s intervals (monitoring.md:57-63), the wake nudge not leading with Esc because the watcher pane is never on a permission prompt (monitoring.md:20-23, 46-47), 'never keystrokes a watched pane' (monitoring.md:71-72), spawned-first + 'ready: monitor live' (monitoring.md:187-193), and no-monitor-stop teardown (monitoring.md:199-208). docs/how-to/mixed-backend-team.md:33-41 is a third full restatement of the same model (watched set, intervals, first-spawn, 'the Director never runs the monitor itself'). Both paragraphs already link to [Monitoring](../concepts/monitoring.md), yet spell the content out anyway — violating the project's own SSOT style rule (docs/get-started/contributing.md:87-88: 'one fact, one home. When another page needs the fact, link').
- **Proposed change**: In each how-to page, keep the actionable parts (the `member create --role monitor` command block and `monitor status` check in monitor-and-recover.md) plus one linking sentence ('the tick comes from the monitoring member's `cafleet monitor` loop — see [Monitoring](../concepts/monitoring.md)'), and delete the restated mechanics (intervals, Esc semantics, wake-nudge details, teardown rules).
- **Why behavior-preserving**: Documentation-only change; every deleted fact remains canonically stated on docs/concepts/monitoring.md, which both pages already link to. No CLI, API, or output text is described differently afterward.
- **Verifier notes**: All cited facts verified: monitor-and-recover.md:11-45 and mixed-backend-team.md:33-41 restate the monitoring model that is canonical on docs/concepts/monitoring.md (watched set/intervals at :55-63, no-Esc wake nudge at :20-23 and :46-47, never-keystrokes-a-watched-pane at :71-72, spawned-first + 'ready: monitor live' at :186-193, teardown/no-monitor-stop at :199-208). The SSOT rule at contributing.md:87-88 is quoted accurately, and both pages already link to the concepts page. Doc-only change; no CLI/API surface described differently. One scope correction: the parenthetical at monitor-and-recover.md:23-24 — the monitor's canonical spawn prompt lives in the /cafleet skill's roles/monitor.md — is the ONLY docs/ mention of that pointer (grep confirms) and is not on monitoring.md; keep that pointer (e.g. next to the retained command block) when trimming. Note also the retained command block uses --model sonnet while monitoring.md says the Director passes --model haiku — a pre-existing inconsistency unaffected by this change.

### [docs-2] Two conflicting Claude Code permission-allowlist recipes inside docs/
- **Status**: REFUTED/RESCOPED
- **Kind**: doc-redundancy
- **Location**: `docs/spec/cli-options.md:103-119 (vs docs/get-started/configure.md:17-37)`
- **Estimated impact**: -15 lines in cli-options.md; removes a direct contradiction between two docs pages
- **Evidence**: docs/get-started/configure.md:17-37 prescribes the recommended Claude Code config: a single `"Bash(cafleet *)"` allow entry plus `"Bash(cafleet * member exec *)"` under ask, and explains 'The Bash(cafleet *) pattern is the single allow-everything entry … one pattern covers every subcommand for every fleet.' docs/spec/cli-options.md:103-119 (§ permissions.allow coverage) prescribes a different recipe for the same file: 'The allow set is generated mechanically, one Bash(...) pattern per allow-listed subcommand … Bash(cafleet <grp> <cmd> --fleet-id *) … Apply the patterns to your user-level ~/.claude/settings.json manually', plus per-`--json` companion patterns, and cli-options.md:91 hangs the 'canonical flag order' claim on it. This section has no SPEC.md counterpart (grep for permissions.allow in SPEC.md matches nothing), so it is not the deliberate SPEC↔docs mirroring — it is operator guidance duplicated (and contradicted) across two docs pages.
- **Proposed change**: Make configure.md the single home for the recommended settings.json recipe. Replace cli-options.md § 'permissions.allow coverage' with a short link to [Configure](../get-started/configure.md) (keeping only the spec-relevant fact that `member exec` should stay under ask and that `--json` precedes the subcommand name), and soften the flag-order sentence at cli-options.md:91 to reference the pattern-matching caveat where the recipe lives.
- **Why behavior-preserving**: Neither recipe is shipped by the repo (cli-options.md:119: 'the repo does not ship a committed .claude/settings.json permissions block'); this consolidates operator guidance without changing any cafleet behavior, flag, or output. SPEC.md is untouched since it never contained this section.
- **Verifier notes**: The factual observations are mostly accurate (configure.md:17-37 recommends the single Bash(cafleet *) allow; cli-options.md:103-119 documents a per-subcommand pattern set; SPEC.md has no permissions.allow counterpart — verified by grep). But the proposed change is refuted on two grounds. (1) Missed caller: skills/cafleet/SKILL.md:78 explicitly links to docs/spec/cli-options.md#permissionsallow-coverage 'for the pattern set'; replacing the section with a link to configure.md orphans that load-bearing skill cross-reference, and skill drift is a declared blocker per .claude/rules/documentation-maintenance.md. (2) Not deduplication: the two pages are alternative recipes at different granularity, not two copies of one fact — the fine-grained pattern-set facts (the mechanical one-pattern-per-subcommand rule, the flag-order dependence hung on it at cli-options.md:54 and :91, the --json companion-pattern rule) do not exist in configure.md, so 'every deleted fact remains elsewhere' is false; the flag-order fact would lose its home ('soften ... to reference the pattern-matching caveat where the recipe lives' fails because configure.md's Bash(cafleet *) recipe is order-insensitive and carries no such caveat). 'Direct contradiction' also overstates it: cli-options.md never presents its set as the recommended config, and both agree member exec stays under ask. A reconciliation (e.g. cross-links stating the relationship) may be worthwhile, but not the proposed removal.

### [docs-3] README describes the message CLI surface twice
- **Status**: REFUTED/RESCOPED
- **Kind**: doc-redundancy
- **Location**: `README.md:214-222 (vs 171-180)`
- **Estimated impact**: -10 lines in README.md
- **Evidence**: README §4 already has the `message` group table (lines 171-180: send/broadcast/poll/ack/cancel/show with their flags). README §5 then repeats five of the same commands in a second table, 'Message broker operations (CLI)' (lines 214-222), restating 'Send a unicast message', 'Fetch un-acked messages', 'Mark a message consumed', 'Retract an un-acked message (sender only)'. The only information not already elsewhere in the README is 'excluding Administrators' on broadcast; the inline-preview note duplicates the Features bullet at line 13.
- **Proposed change**: Delete the §5 'Message broker operations (CLI)' table; fold the one unique fact ('broadcast fans out to all fleet agents excluding the Administrator') into the §4 `message broadcast` row.
- **Why behavior-preserving**: Same-surface deduplication inside README.md; the retained §4 table plus the one folded fact preserves every statement. No described CLI behavior changes, and the README/SPEC/docs mirroring is untouched.
- **Verifier notes**: The duplication is factually accurate: README §4 lines 173-180 and §5 lines 214-222 both list send/broadcast/poll/ack/cancel, the inline-preview note duplicates the Features bullet at line 13, and 'excluding Administrators' is the one unique fact. However, the proposed deletion is refuted by a contract the scanner missed: .claude/skills/update-readme/SKILL.md (the skill that maintains README.md) mandates the README structure at line 47 — section 6 'API Overview -- REST Registry API endpoints and message-broker operations'. Deleting the 'Message broker operations (CLI)' table removes the message-broker-operations half of that mandated section, so the change is not drift-safe: the next /update-readme run works against a structure spec the README no longer satisfies and would tend to reintroduce the table. To make this valid the change must also amend the update-readme skill's README Structure (or retain a minimal message-broker-operations pointer under API Overview), which is outside the finding's claimed scope of a same-file README-only deduplication.

### [docs-4] member-lifecycle.md states the spawn-prompt substitution spec twice on the same page
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `docs/concepts/member-lifecycle.md:52-57 and 71-79`
- **Estimated impact**: -5 lines; one fewer copy of the four-placeholder list to keep in sync (it currently appears in 5+ docs locations)
- **Evidence**: § 'Atomic create flow' (lines 52-57): 'Identity reaches the spawned pane as literals rendered into the prompt: `cafleet member create` runs `str.format` over the resolved prompt, substituting `{fleet_id}`, `{agent_id}` (the member's own newly-allocated id), `{director_agent_id}`, and `{coding_agent}` — see [Coding agents](coding-agents.md).' § 'Spawn-prompt input modes' (lines 74-79) repeats it verbatim in substance: '`cafleet member create` then runs `str.format` over the resolved body, substituting the four identity placeholders (`{fleet_id}`, `{agent_id}`, `{director_agent_id}`, `{coding_agent}`)…'. The canonical spec of both the input modes and the substitution lives at docs/spec/cli-options.md:639-664, which the second section already links to.
- **Proposed change**: Keep the substitution sentence once (in 'Atomic create flow') and reduce 'Spawn-prompt input modes' to the `--text` / `--text-file` / stdin sentence plus its existing link to CLI options, dropping the repeated placeholder list.
- **Why behavior-preserving**: Removes a same-page repetition only; the fact remains on the same page (once) and canonically in docs/spec/cli-options.md. No contract text changes.
- **Verifier notes**: Verified: the str.format + four-placeholder substitution is stated at member-lifecycle.md:53-57 (Atomic create flow) and repeated at :75-79 (Spawn-prompt input modes). The canonical spec exists exactly where cited — docs/spec/cli-options.md:639-664 (§ Spawn-prompt input modes at :639-651 and § Spawn-prompt substitution at :653-664, including the placeholder table and brace-doubling rule) — and the second section already links there. Doc-only, same-page dedup; every fact remains once on the page and canonically in the spec. Minor scope note: the reduced 'Spawn-prompt input modes' paragraph currently also carries the literal-brace-doubling caveat ({{ / }}); it is preserved canonically at cli-options.md:664 behind the retained link, but an implementer could keep that half-sentence to avoid losing the page-local caveat.

### [docs-5] Stale `--id` flag in token-reduction.md — no such CLI option exists
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `docs/concepts/token-reduction.md:19`
- **Estimated impact**: 1 token; removes a stale flag reference from the concepts surface
- **Evidence**: Line 19: 'so they paste straight into `--to` / `--id` / `--agent-id` / `--task-id` with no prefix resolution.' No cafleet subcommand accepts `--id`: grep for `--id` across cafleet/src/cafleet/cli returns nothing; the id options are `--fleet-id`, `--agent-id`, `--member-id`, `--task-id`, `--to` (docs/spec/cli-options.md:95 lists exactly '`--to`, `--member-id`, and `--task-id`'). This is the only `--id` mention in README/SPEC/docs.
- **Proposed change**: Replace `--id` with `--member-id` (or simply drop it) in the flag list on line 19.
- **Why behavior-preserving**: One-token doc correction removing a reference to a flag that does not exist; nothing else cites `--id`.
- **Verifier notes**: Verified: docs/concepts/token-reduction.md:19 lists `--id` among paste-target flags; grep across cafleet/src, docs/, skills/, README.md, and SPEC.md finds no `--id` option anywhere else — the CLI source defines no '--id' option, and cli-options.md:95 names the id options as exactly --to, --member-id, and --task-id (plus --fleet-id/--agent-id). token-reduction.md:19 is the sole occurrence in the repo's user-facing surfaces. Replacing `--id` with `--member-id` (or dropping it) is an accurate one-token doc correction with no cross-references affected.

### [docs-6] contributing.md describes skills/ via nonexistent 'Claude Code and Codex manifests'
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `docs/get-started/contributing.md:18`
- **Estimated impact**: 1 line rewrite; removes the last user-facing mention of the removed plugin-manifest mechanism
- **Evidence**: Project-structure table row: '| `skills/` | Plugin skills shared by the Claude Code and Codex manifests. |'. No plugin/marketplace manifest files exist in the tree (git ls-files for plugin/marketplace patterns matches only historical design-docs, including 0000110-remove-marketplace-install-instructions), skills now reach agent homes via `cafleet setup` (release zip, docs/get-started/install.md:24-32) or `mise //:skill-install` (mise.toml:22-28), and the row also omits the third backend (`opencode`) that every other page names. README.md:246 already has the accurate wording: 'Coding-agent skill files (cafleet, cafleet-design-doc, cafleet-research)'.
- **Proposed change**: Rewrite the row to match reality, e.g. '| `skills/` | Coding-agent skill files (`cafleet`, `cafleet-design-doc`, `cafleet-research`), installed into the agent homes by `cafleet setup` / `mise //:skill-install`. |'.
- **Why behavior-preserving**: Doc-only correction of a description that references a removed distribution mechanism; the replacement wording restates what install.md and mise.toml already document.
- **Verifier notes**: Verified: docs/get-started/contributing.md:18 reads exactly as cited. git ls-files for plugin/marketplace/manifest patterns matches only historical design-docs (including 0000048-codex-plugin-manifest and 0000110-remove-marketplace-install-instructions) — no manifest files exist in the tree. Skills reach agent homes via `cafleet setup` (release zip extraction into the three agent homes, docs/get-started/install.md:24-32) and `mise //:skill-install` for contributors (mise.toml:22-27, running `gh skill install ./ --from-local` for claude-code/codex/opencode). README.md:246 already carries the accurate wording the rewrite mirrors, and the row indeed omits the opencode backend. This is also the last user-facing 'manifest' mention for the removed distribution mechanism (the only other hit, contributing.md:19, is about bun toolchain manifests — unrelated). The proposed rewrite is a doc-only correction consistent with the removal rule.

### [docs-7] opencode reference page carries historical design-doc narration ('Step 0 GATE')
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `docs/reference/coding-agents/opencode.md:62, 129-132`
- **Estimated impact**: -2 lines of prose; opencode reference page becomes self-contained
- **Evidence**: Line 62: 'cafleet has been validated against `opencode 1.15.5` (the version installed at the time Step 0 empirical verification was completed on 2026-05-19)'. Lines 130-132 (verification recipe comment): 'If it does NOT, the safety floor is broken — STOP and re-run the agent-load smoke from the design doc's Step 0 GATE.' 'Step 0' is an internal step of design-docs/0000067-opencode-backend, never defined anywhere in docs/ — a reader of this operator page cannot resolve the pointer, and the parenthetical is historical narration of the drafting process, not a statement of current behavior.
- **Proposed change**: Line 62: trim to 'cafleet has been validated against `opencode 1.15.5`; `1.15.5` is also the minimum supported version.' Lines 130-132: replace the design-doc pointer with a self-contained instruction, e.g. 'STOP and inspect `~/.opencode/agents/cafleet.md` — the preset's deny ruleset is not being applied.'
- **Why behavior-preserving**: Removes narration and an unresolvable cross-reference; the validated version, the minimum-version statement, and the STOP-on-regression instruction are all preserved.
- **Verifier notes**: Verified: opencode.md:62 and :130-132 read exactly as cited, and grep confirms 'Step 0' in this sense is defined nowhere in docs/ — it lives only in design-docs/0000067-opencode-backend/design-doc.md (§ 'Step 0: Empirical TUI verification' at :460, with the GATE agent-load + deny-list smoke checkbox at :466). One overstatement to correct: the pointer is not strictly unresolvable — design-docs/ is committed in this repo (project git-workflow override) and browsable on GitHub — but it is absent from the published docs site and is drafting-process narration in an operator page, consistent with the project's rule keeping historical records in design docs. The proposed replacements preserve the substance: line 62 keeps 'validated against opencode 1.15.5' + minimum-version; the STOP instruction remains, and 'inspect ~/.opencode/agents/cafleet.md — the preset's deny ruleset is not being applied' accurately restates the failure mode the GATE targets (per the design doc, an agent-definition parse failure makes opencode silently fall back to its catch-all-allow default build agent), with the page's § Permission-popup recovery posture retaining the escalation path.

## Area: output-webui (3/6 confirmed)

### [output-webui-1] Monitor runtime payload builder duplicated between CLI and WebUI
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/cli/monitor.py:74-101`
- **Estimated impact**: -28 lines, single source of truth for the runtime shape
- **Evidence**: cafleet/src/cafleet/cli/monitor.py:74-101 builds the runtime dict ({"running", "pid", "tick_seconds", "last_tick_at", "last_tick_age_seconds", "started_at"}) via broker.read_monitor_runtime + broker.monitor_is_live + the same fromisoformat age computation as cafleet/src/cafleet/webui/api.py:24-53 (_monitor_runtime_payload). The CLI code even carries the comment '(matches GET /api/monitor for WebUI/CLI parity)' (monitor.py:79), and the two branches are line-for-line identical in keys, key order, and value derivation.
- **Proposed change**: Extract one shared helper (natural home: the broker package next to read_monitor_runtime/monitor_is_live, e.g. broker.monitor_runtime_payload(fleet_id, now)) and call it from both cli/monitor.py monitor_status and webui/api.py _monitor_runtime_payload / GET /api/monitor.
- **Why behavior-preserving**: Both call sites construct the exact same dict with the same key order and the same null-when-stale semantics; a shared function returning that dict yields byte-identical CLI JSON (format_json of the same dict) and identical FastAPI JSON serialization.
- **Verifier notes**: Verified: cli/monitor.py:74-101 and webui/api.py:24-53 build the identical dict (same keys, key order, null-when-stale semantics, same fromisoformat age computation); the CLI comment at monitor.py:79 explicitly claims parity with GET /api/monitor. A shared broker helper taking (fleet_id, now) preserves both call sites' behavior exactly (CLI reuses one `now` for runtime+agents; WebUI passes its own). SPEC's dependency graph (cli->broker, webui->broker) permits the helper's home in broker. Tests exercise both paths via real DB rows (claim_monitor_runtime), not monkeypatched broker functions, so no test-fragility hazard. Scope correction: docs/api/broker.md:22 enumerates broker/monitor.py's functions and SPEC.md ~770 lists the broker monitor surface — the new helper must be added to both in the same cycle per documentation-maintenance.md, a routine additive doc sync.

### [output-webui-2] client_command decorator carries dead configuration branches (truncates_task_text always True, text_formatter always provided)
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/cli/_helpers.py:93-151`
- **Estimated impact**: -12 lines in _helpers.py plus 6 one-line arg removals in message.py; simpler decorator contract
- **Evidence**: All six call sites are in cafleet/src/cafleet/cli/message.py (lines 36, 69, 93, 112, 132, 150) and every one passes truncates_task_text=True and a text_formatter. Therefore in _helpers.py the `else: rendered = result` branch (131-132), the `text_formatter(result)` no-kwargs branch (139-140), the final `else: click.echo(output.format_json(rendered))` fallback (141-142), and the docstring's 'Otherwise → JSON output is the raw broker result' case (105-106) are unreachable in production.
- **Proposed change**: Drop the truncates_task_text parameter (inline its True behavior), make text_formatter a required Callable, delete the three dead branches and the obsolete docstring case, and remove `truncates_task_text=True` from the six decorators in message.py.
- **Why behavior-preserving**: Every existing invocation already takes the truncates_task_text=True + text_formatter path; removing the never-taken branches changes no observable CLI output for any command.
- **Verifier notes**: The production-scope facts check out (all six src call sites in cli/message.py pass truncates_task_text=True plus a text_formatter), but the branches are NOT dead at repo scope and the change is not contract-preserving as proposed. (1) tests/cli/test_client_command.py builds three harness commands that exercise exactly the branches slated for deletion: line 35 uses text_formatter without truncates_task_text (hits the `rendered = result` branch and the no-kwargs `text_formatter(result)` call shape — its lambda takes no `full` kwarg and would TypeError under the inlined-True behavior), and line 55 uses bare `client_command()` (hits the `else: format_json(rendered)` fallback and would be invalid once text_formatter is required). Six of the file's seven tests route through these configurations. (2) SPEC.md:908-926 documents the wrapper contract with 'a text renderer (optional)' and emit step 5 'else emit JSON' — the proposal deletes documented contract behavior without the mandatory same-cycle SPEC edit. (3) design-docs/0000056 P8 previously evaluated collapsing this wrapper's branches and recorded 'skip' partly because the decorator is imported directly by its test module. This is a contract-plus-test rewrite, not a behavior-preserving dead-code removal.

### [output-webui-3] Hand-rolled preview truncation in broker/messaging duplicates output.render.truncate_text
- **Status**: REFUTED/RESCOPED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/broker/messaging.py:36-42`
- **Estimated impact**: -6 lines including the mirror-contract comment; one truncation implementation instead of two
- **Evidence**: messaging.py:40-42: `preview_text = task_dict["text"]; if len(preview_text) > settings.max_text_len: preview_text = preview_text[: settings.max_text_len] + "…"` with a comment (37-39) explicitly stating it 'Mirrors output.truncate_text's contract: same limit ... and same single-codepoint U+2026 suffix'. cafleet/src/cafleet/output/render.py:39-53 truncate_text(value, full=False, limit=None) implements exactly that: settings.max_text_len fallback, `<=` keep, slice + _TRUNCATION_SUFFIX ("…").
- **Proposed change**: Replace the three lines with `preview_text = truncate_text(task_dict["text"], full=False)` (import from cafleet.output.render) and delete the 'Mirrors ... contract' comment, which exists only because of the duplication.
- **Why behavior-preserving**: For a str input the two implementations are codepoint-for-codepoint identical (same limit source, same <= boundary, same U+2026 suffix); no import cycle since cafleet.output imports only cafleet.config, never broker.
- **Verifier notes**: The duplication is factually real and the runtime output would be codepoint-identical for all reachable inputs (same settings.max_text_len source, complementary >/<= boundary, same U+2026 suffix), and there is indeed no import cycle (cafleet.output imports only cafleet.config). But the mirror is deliberate, documented architecture: SPEC.md:174-196 specifies the module dependency graph with broker depending on db + multiplexer + config ONLY (line 186-188 explicitly lists 'config (max_text_len for preview truncation)' as the reason broker has the config edge), and SPEC.md:181-183/199-206 specify that preview truncation 'is done by the broker' precisely so the layering holds. Importing cafleet.output.render into broker/messaging.py adds a broker->output dependency edge that contradicts SPEC's documented dependency contract — the 'Mirrors output.truncate_text's contract' comment exists to keep the parity without the cross-layer import, not merely as duplication residue. Since SPEC drift is a repo-declared blocker and the finding claims the change is strictly behavior/contract-preserving without amending the architecture section, it is refuted as scoped. (A variant that keeps truncation broker-local or moves the shared primitive below both layers could be legitimate, but that is a different, larger change.)

### [output-webui-4] _agent_id_for_column is a trivial str() wrapper
- **Status**: CONFIRMED
- **Kind**: redundant-abstraction
- **Location**: `cafleet/src/cafleet/output/formatters.py:164-165`
- **Estimated impact**: -4 lines, one fewer private helper
- **Evidence**: formatters.py:164-165: `def _agent_id_for_column(agent_id: int) -> str: return str(agent_id)` — a one-line passthrough. Its only two callers (formatters.py:185 in format_member_list_activity, formatters.py:325 in format_member_list) immediately interpolate the result; sibling formatters format_member_roster (line 303) and format_monitor_status (line 255) already inline `str(a['agent_id'])` directly for the same purpose.
- **Proposed change**: Delete _agent_id_for_column and inline `str(m["agent_id"])` at the two call sites, matching the style already used by format_member_roster and format_monitor_status.
- **Why behavior-preserving**: str(agent_id) is literally the function body; the rendered table text is byte-identical.
- **Verifier notes**: Verified: formatters.py:164-165 is a one-line str() passthrough; its only callers are formatters.py:185 and formatters.py:325, and siblings format_member_roster (line 303) and format_monitor_status (line 255) already inline str(a['agent_id']) for the same table-column purpose. No other src, test, or user-facing-doc references exist (design-docs mentions are historical record). Rendered table text is byte-identical since the {:<width} padding stays at the call sites. Scope correction: SPEC.md:1472-1473 lists 'an agent-id stringifier' among the formatter layer's 'Private contract helpers' — that prose enumeration should be trimmed in the same change per documentation-maintenance.md; the observable text-layout contract is untouched.

### [output-webui-5] test_format_indexed_list__empty_and_non_empty_join_behaviour largely duplicates the format_indexed_list coverage in test_compact_formatters
- **Status**: CONFIRMED
- **Kind**: redundant-test
- **Location**: `cafleet/tests/output/test_indexed_list.py:6-20`
- **Estimated impact**: -15 lines of duplicated test code
- **Evidence**: test_indexed_list.py:6-20 asserts (a) empty list returns the empty message verbatim and (b) non-empty items are formatter-mapped and joined with "\n\n". cafleet/tests/output/test_compact_formatters.py:145-157 (test_format_indexed_list__drops_index_and_blank_line_separated) asserts the identical behaviors: `format_indexed_list([], lambda x: x, "No items found.") == "No items found."` and `rendered == "alpha\n\nbeta\n\ngamma"`, plus the single-item case. The only assertion unique to the test_indexed_list version is that the formatter is not invoked for an empty list (formatter_calls == []).
- **Proposed change**: Delete test_format_indexed_list__empty_and_non_empty_join_behaviour; if the lazy-evaluation assertion (formatter not called on empty input) is considered load-bearing, fold that single assertion into the surviving test in test_compact_formatters.py.
- **Why behavior-preserving**: Tests only; the retained tests in test_compact_formatters.py (plus test_indexed_list.py's byte-identical task/agent-shape test) keep every production behavior of format_indexed_list under assertion.
- **Verifier notes**: Verified: test_compact_formatters.py:145-157 (test_format_indexed_list__drops_index_and_blank_line_separated) asserts empty-list-returns-empty-message verbatim, the \n\n join for three items, and the single-item case; test_compact_formatters.py:160-167 additionally covers multiline formatter output, and test_indexed_list.py:23-51 keeps the task/agent-shape byte-identity coverage. The deleted test's unique coverage is slightly broader than the finding states: not only 'formatter not invoked for empty list' (formatter_calls == []) but also the call-log assertion formatter_calls == ["a","b","c"] proving exactly-once-per-item invocation in order — fold both into the surviving test if considered load-bearing (the finding's escape hatch already provides for this). Tests-only change; no production behavior or documented contract is affected.

### [output-webui-6] truncate_task_text's limit parameter has no production caller
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/output/render.py:56-66`
- **Estimated impact**: -1 parameter and simpler contract; small test adjustments confined to test_output.py
- **Evidence**: The only src call site is cafleet/src/cafleet/cli/_helpers.py:129 `output.truncate_task_text(result, full=full)` — limit is never passed outside the function's own tests (cafleet/tests/output/test_output.py:135-176, which pass limit=10 purely for fixture convenience). truncate_text's limit parameter, by contrast, has a real production caller (formatters.py:75, limit=60).
- **Proposed change**: Drop the `limit` parameter from truncate_task_text (forward `limit=None` implicitly via truncate_text's settings.max_text_len default) and update the tests in test_output.py to set the limit via the existing settings mechanism, as test_truncation_settings.py already does.
- **Why behavior-preserving**: No production code passes limit, so the CLI's truncation output is unchanged; only the helper's signature narrows to its actually-used surface.
- **Verifier notes**: The narrow factual claim is accurate — the only src call site is cli/_helpers.py:129, which never passes limit — but the parameter is not dead at repo scope and its removal is a documented-contract change, not a behavior-preserving signature narrowing. (1) SPEC.md:1463 documents the exact render-function signature 'truncate_task_text(result, full, limit) (in-place)' as part of the reimplementation contract, and SPEC.md:1482's truncation rule ('the effective limit is the explicit limit argument when given, else max_text_len') covers the whole render layer; the proposal omits the mandatory same-cycle SPEC edit that documentation-maintenance.md declares a blocker. (2) Six test call sites in tests/output/test_output.py (lines 135, 143, 148, 154, 171, 176) pass limit=10, including the codepoint-truncation fixtures — per the verification rubric, a parameter referenced by the spec and by tests is not dead code. The rewiring through the settings mechanism is feasible (test_truncation_settings.py shows the pattern) but the change as scoped is a contract narrowing plus test rewrite, so the strictly-behavior-preserving claim is refuted.

## Area: project-config (3/4 confirmed)

### [project-config-1] Unreferenced root mise task `all-install`
- **Status**: REFUTED/RESCOPED
- **Kind**: dead-code
- **Location**: `mise.toml:30-35`
- **Estimated impact**: -6 lines, one fewer undocumented task in `mise tasks` output
- **Evidence**: mise.toml:30-35 defines [tasks.all-install] chaining //admin:build, //cafleet:install, //:skill-install. A repo-wide grep for "all-install" across README.md, SPEC.md, docs/, skills/, .claude/ (including settings.json permissions), CLAUDE.md, CONTRIBUTING.md, and .github/workflows returns zero matches — the only hits are mise.toml itself and historical design-docs/ (out of scope). The .claude/settings.json allow list permits only specific root tasks (//:bun-install, //:docs-build, //:uv-sync, //:slidev), so no agent workflow can even invoke it without a prompt. Its three constituent tasks are each documented and referenced individually (e.g. //:skill-install in README.md:68 and docs/get-started/install.md:86).
- **Proposed change**: Delete the [tasks.all-install] block from the root mise.toml (or, if the maintainer uses it interactively, add a description and a doc reference — but as tracked config it is currently dead).
- **Why behavior-preserving**: No documentation, CI workflow, permission entry, skill, or other mise task references the task; every path it aggregates remains available via the individually documented tasks.
- **Verifier notes**: The grep evidence is factually accurate (only mise.toml:30 and design-docs/0000110 reference it), but the behavior-preservation claim fails. (1) In this repo design-docs/ is committed and tracked (project git-workflow override), and design-doc 0000110 explicitly lists `[tasks.all-install]` in its 'Out of scope / kept' section as part of the retained contributor/local-dev install surface — a recent, deliberate decision to keep it, not stale residue. (2) A mise task is an operator-facing entry point whose only expected caller is a human; deleting it removes a currently working command (`mise //:all-install` chains //admin:build + //cafleet:install + //:skill-install) and changes `mise tasks` output — the finding's own estimated_impact admits this observable change, and its hedged alternative ('if the maintainer uses it interactively') concedes the use case cannot be ruled out. Not strictly behavior-preserving; interactive use by the maintainer is plausible and documented intent says keep.

### [project-config-2] Unused dependency @slidev/theme-seriph in root package.json
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `package.json:13`
- **Estimated impact**: -1 dependency, smaller bun.lock and node_modules
- **Evidence**: package.json:13 declares "@slidev/theme-seriph": "^0.25.0". A grep for "seriph" across all tracked *.md/*.json/*.toml/*.vue/*.ts (excluding node_modules, researches/, design-docs/) matches only this line. The research skill's Slidev decks use the repo-local custom theme instead: skills/cafleet-research/reference/slidev.md:9 pins `theme: <cafleet-plugin-install-dir>/skills/cafleet-research/reference/slidev/theme`, and that local theme (components/, layouts/, styles/) contains no reference to seriph. @slidev/theme-default remains needed as Slidev's implicit fallback when a deck omits `theme:`, but nothing selects seriph.
- **Proposed change**: Remove "@slidev/theme-seriph" from dependencies and refresh bun.lock.
- **Why behavior-preserving**: No tracked slide deck, skill, or config selects the seriph theme; the documented deck workflow pins the local custom theme path. (Caveat: gitignored decks under researches/ could not be checked — a quick local grep there before removing closes that gap.)
- **Verifier notes**: Verified: a case-insensitive repo-wide grep for 'seriph' (including docs/, skills/, SPEC.md, README.md, design-docs/, .claude/) hits only package.json:13 (plus bun.lock). The finding's caveat about gitignored researches/ decks is moot on this machine — researches/ does not exist locally. skills/cafleet-research/reference/slidev.md:9 pins the repo-local theme path, and that theme (skills/cafleet-research/reference/slidev/theme) contains no 'extends' or seriph reference, so no theme-inheritance chain pulls seriph in. Decks omitting `theme:` resolve to @slidev/theme-default, which is retained. Removing the dependency and refreshing bun.lock preserves every documented workflow.

### [project-config-3] Two settings.json deny entries fully subsumed by broader wait denies
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `.claude/settings.json:38-39`
- **Estimated impact**: -2 lines, shorter deny list to reason about
- **Evidence**: Deny list contains both "Bash(bun run agent-browser wait --load networkidle)" (line 38) and "Bash(bun run agent-browser wait --load networkidle *)" (line 39), while lines 45-46 contain "Bash(bun run agent-browser wait)" and "Bash(bun run agent-browser wait *)". The prefix-wildcard pattern on line 46 (`wait *`) matches every command the two networkidle lines match (`wait --load networkidle` and `wait --load networkidle <anything>`). The session-scoped variants on lines 36-37 are NOT subsumed (line 44's `--session vr-batch-* wait` has no trailing wildcard) and must stay.
- **Proposed change**: Delete the two bare `wait --load networkidle` deny entries (lines 38-39), keeping lines 36-37 and 44-46.
- **Why behavior-preserving**: Every command string the removed patterns deny is already denied by "Bash(bun run agent-browser wait *)"; the deny outcome for all invocations is identical. The prose in .claude/rules/commands.md ("blocks the common `wait --load networkidle` forms and bare `wait` forms") remains true.
- **Verifier notes**: Verified line-by-line: .claude/settings.json lines 38-39 are 'Bash(bun run agent-browser wait --load networkidle)' and '... networkidle *)'; line 46 'Bash(bun run agent-browser wait *)' prefix-matches every command string those two match (both start with 'bun run agent-browser wait '), and line 45 covers the bare 'wait'. Deny outcome is identical for all invocations. The finding correctly identifies that session-scoped lines 36-37 ('--session * wait --load networkidle') are NOT subsumed (they don't start with 'wait', and line 44 has no trailing wildcard) and keeps them. Cross-references checked: .claude/rules/commands.md and skills/cafleet-research/presentation/roles/visual-reviewer.md:94 describe the wait-deny family generically ('blocks the common wait --load networkidle forms and bare wait forms') and remain accurate after the removal; design-docs 0000057/0000058 mentions are historical. Only observable delta is which rule string a denial message cites, which is not a documented contract.

### [project-config-4] .bumpversion.toml restates bump-my-version defaults and carries an inert hook flag
- **Status**: CONFIRMED
- **Kind**: simplifiable-logic
- **Location**: `.bumpversion.toml:3-8`
- **Estimated impact**: -5 lines (~23 → ~18), config states only the non-default intent
- **Evidence**: Lines 3-4 set parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)" and serialize = ["{major}.{minor}.{patch}"] — both are bump-my-version's documented defaults verbatim. Line 5 allow_dirty = false and line 7 message = "Bump version: {current_version} → {new_version}" are also the tool defaults. Line 8 allow_shell_hooks = true is inert: no setup_hooks / pre_commit_hooks / post_commit_hooks are defined anywhere in the file, so the flag gates nothing.
- **Proposed change**: Drop parse, serialize, allow_dirty, message, and allow_shell_hooks, keeping current_version, commit = true (non-default), and the two [[tool.bumpversion.files]] blocks.
- **Why behavior-preserving**: Removing settings that equal the tool's defaults leaves every bump-my-version invocation producing byte-identical results; allow_shell_hooks has no effect when no hooks are configured.
- **Verifier notes**: Verified against the actually installed bump-my-version 1.3.0 (pinned in uv.lock, so defaults are stable): DEFAULTS has parse='(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)', serialize=['{major}.{minor}.{patch}'], allow_dirty=false, message='Bump version: {current_version} → {new_version}' — all byte-identical to lines 3-5 and 7. One correction to the evidence: allow_shell_hooks defaults to TRUE in bump-my-version (both DEFAULTS and the Config model), so line 8 restates the default rather than merely being inert — the 'inert because no hooks are defined' argument is also true (no setup_hooks/pre_commit_hooks/post_commit_hooks anywhere), but the default-equality makes removal safe unconditionally. commit=true is genuinely non-default (default false) and is correctly retained along with current_version and both [[tool.bumpversion.files]] blocks. No README/SPEC/docs/skills/CI reference these config fields (only historical design-docs). Behavior-preserving.

## Area: runtime (3/4 confirmed)

### [runtime-1] Triplicated best-effort keystroke boilerplate in TmuxMultiplexer send_poll_trigger / send_wake_trigger / send_inline_preview
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/src/cafleet/multiplexer/tmux.py:203-306`
- **Estimated impact**: -15 to -20 lines in tmux.py; one code path instead of three to maintain
- **Evidence**: All three methods repeat the identical wrapper: `if shutil.which("tmux") is None: return False` (tmux.py:213-214, 246-247, 281-282), then `try: _send_literal_then_enter(target_pane_id=..., payload=..., timeout=5, esc_first=...)` / `except TmuxError: return False` / `return True` (tmux.py:216-225, 261-269, 297-306). The only per-method differences are the payload string and the `esc_first` flag.
- **Proposed change**: Add a module-level helper, e.g. `_best_effort_send(*, target_pane_id: str, payload: str, esc_first: bool = False) -> bool`, holding the shutil.which pre-check, the `_send_literal_then_enter(..., timeout=5, esc_first=esc_first)` call, and the TmuxError→False conversion. Each of the three methods reduces to payload construction plus one helper call.
- **Why behavior-preserving**: Pure factoring: the exact same statements execute in the same order with the same arguments, return values (True/False), and exception handling. Existing tests (tests/multiplexer/test_tmux.py:357-423, 712-772 and test_tmux_send_inline_preview.py) monkeypatch `shutil.which`, `time.sleep`, and module-level `_run`, so they pass unchanged — including the `tmux_binary_missing_returns_false` cases that assert no `_run` call, since the which pre-check is retained inside the helper.
- **Verifier notes**: Verified against tmux.py:203-306. All three methods carry the identical wrapper (shutil.which pre-check at 213-214/246-247/281-282; try/_send_literal_then_enter(timeout=5)/except TmuxError: return False/return True at 216-225/261-269/297-306), differing only in payload and esc_first (send_wake_trigger relies on the default False; the proposed helper's esc_first=False default preserves that). Tests at tests/multiplexer/test_tmux.py:357-423 and 712-772 and test_tmux_send_inline_preview.py monkeypatch 'shutil.which', 'time.sleep', and module-level multiplexer_tmux._run — all unaffected by a same-module helper, including the tmux_binary_missing_returns_false cases asserting zero _run calls (the which pre-check moves into the helper intact). SPEC.md §6.5 specifies each method's observable best-effort semantics (tmux missing → false, any error → false, timeout 5s, Esc-first matrix), all preserved by the factoring. No doc/skill references the internal structure. Strictly behavior-preserving.

### [runtime-2] Dead re-exports: poll_until_pane_gone and ensure_binary_on_path in package __init__ files
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `cafleet/src/cafleet/multiplexer/__init__.py:1-5, 18 (multiplexer); 1, 18 (coding_agent)`
- **Estimated impact**: -4 lines; smaller advertised package surface
- **Evidence**: multiplexer/__init__.py imports `poll_until_pane_gone` from base (line 4) and lists it in `__all__` (line 18), but a whole-tree grep shows the only consumer is tmux.py, which imports it directly from `cafleet.multiplexer.base` (tmux.py:7); no src or test file imports it from the package. Likewise coding_agent/__init__.py re-exports `ensure_binary_on_path` (lines 1, 18), but all three backends import it from `cafleet.coding_agent.base` (claude.py:1, codex.py:1, opencode.py:1) and no test imports it at all (the only test hit is a docstring mention in tests/coding_agent/test_opencode.py:225).
- **Proposed change**: Drop `poll_until_pane_gone` from the multiplexer package import and `__all__`, and drop `ensure_binary_on_path` from the coding_agent package import and `__all__`. Both symbols stay in their base modules where every real consumer already imports them.
- **Why behavior-preserving**: These are internal (non-CLI, non-HTTP, non-schema) re-exports with zero importers via the package path anywhere in src/ or tests/; removing the aliases changes no executed code.
- **Verifier notes**: Verified. multiplexer/__init__.py imports poll_until_pane_gone (line 4) and lists it in __all__ (line 18); the only consumer is tmux.py:7 which imports it from cafleet.multiplexer.base directly. coding_agent/__init__.py re-exports ensure_binary_on_path (lines 1, 18); all three backends (claude.py:1, codex.py:1, opencode.py:1) import it from cafleet.coding_agent.base. Whole-repo grep (src, tests, docs/, skills/, README.md, SPEC.md) finds zero imports of either symbol via the package path — the only test hit is a docstring mention at tests/coding_agent/test_opencode.py:225 (no import; that file imports only CODING_AGENTS, CodingAgent, OpencodeAgent). SPEC.md documents the poll_until_pane_gone helper semantics and the shared binary-probe helper behavior, both of which stay in their base modules; SPEC does not document package __all__/re-export surfaces, so no SPEC drift. Design-doc 0000066 mentions the __init__ exposure but design docs are the historical record per project rules. Behavior-preserving.

### [runtime-3] Redundant byte-exact spawn-argv and validate_model tests duplicated within/across coding_agent test files
- **Status**: CONFIRMED
- **Kind**: redundant-test
- **Location**: `cafleet/tests/coding_agent/test_protocol.py:62-72, 95-117; test_opencode.py 33-43, 51-62, 148-150, 180-193`
- **Estimated impact**: ~-60 lines across two test files; fewer places to update when a spawn argv legitimately changes
- **Evidence**: Within test_protocol.py: `test_claude_build_spawn_argv__byte_exact` (62-72) asserts `build_spawn_argv("PROMPT_TEXT", display_name="Bob")` equals the exact 6-token list — the identical call and identical expected list as the parametrized `test_build_spawn_argv__model_omitted_is_byte_identical_to_pinned` claude case via `_PINNED_ARGV_WITHOUT_MODEL["claude"]` (131-162). Same for codex (95-107 vs 140-147; the extra `assert "--name" not in argv` is implied by exact list equality), and `test_codex_build_spawn_argv__permission_tokens_precede_prompt` (110-117) is fully implied by the byte-exact equality in the same file. Across files: test_opencode.py `test_build_spawn_argv_byte_exact` (51-62) == `_PINNED_ARGV_WITHOUT_MODEL["opencode"]` parametrized case (148-162); `test_build_spawn_argv_with_model_byte_exact` (test_opencode.py:180-193) is assertion-identical to `test_opencode_build_spawn_argv__model_set_byte_exact` (test_protocol.py:206-219); `test_validate_model_accepts_none` (test_opencode.py:148-150) duplicates the opencode case of `test_validate_model__none_is_always_valid` (test_protocol.py:234-237); `test_opencode_satisfies_coding_agent_protocol` + `test_opencode_agent_name_and_binary_name` (test_opencode.py:33-43) duplicate the parametrized `test_impl_satisfies_protocol` opencode case (test_protocol.py:31-34, which also checks `impl.name == name`).
- **Proposed change**: Delete one copy of each duplicated assertion — e.g. keep the parametrized pinned-argv tests in test_protocol.py as the single byte-exact source of truth and delete the verbatim duplicates (claude/codex standalone byte-exact tests, the codex ordering test, and the five duplicated opencode tests). Keep opencode-only regression guards (`run`/`--interactive`/`--dangerously-skip-permissions` absence, `--prompt`-flag positioning, display_name-equivalence) which add coverage the pinned list already implies only partially in their docstrings.
- **Why behavior-preserving**: Test-only change; every deleted assertion is byte-identical (same call arguments, same expected list/None) to a retained assertion, so coverage of the production contract is unchanged.
- **Verifier notes**: Substantially valid — production behavior is untouched and every deleted contract fact remains pinned by a retained test — but the evidence overclaims 'byte-identical (same call arguments)' in three places, and one deletion needs a scope correction. Exact duplicates confirmed: claude standalone byte-exact (test_protocol.py:62-72, display_name='Bob') == pinned claude case; test_build_spawn_argv_with_model_byte_exact (test_opencode.py:180-193) is assertion-identical to test_protocol.py:206-219; test_validate_model_accepts_none (test_opencode.py:148-150) duplicates the opencode None case (test_protocol.py:234-237); test_opencode_satisfies_coding_agent_protocol duplicates the parametrized isinstance check. NOT byte-identical calls: codex standalone byte-exact uses display_name='ignored' vs pinned 'Bob'; opencode standalone byte-exact likewise; the codex ordering test uses prompt 'hello'/display 'x' — same code path, and the pinned lists (with 'Bob' absent from argv) still prove display_name is ignored, so coverage is equivalent though not verbatim. Correction: test_opencode_agent_name_and_binary_name's `binary_name == "opencode"` assertion has no direct retained duplicate (test_impl_satisfies_protocol checks only isinstance and impl.name == name); it is covered only transitively via test_build_spawn_argv_includes_prompt_last (argv[0] == impl.binary_name) plus the pinned argv (argv[0] == 'opencode') — any regression combination still fails the retained suite, but keeping that one 6-line test (or folding the binary_name assertion into a kept test) is the safer scope.

### [runtime-4] Multiplexer Protocol declares pane_exists but no caller uses it through the Protocol
- **Status**: REFUTED/RESCOPED
- **Kind**: redundant-abstraction
- **Location**: `cafleet/src/cafleet/multiplexer/base.py:90-92`
- **Estimated impact**: -3 lines plus docstring; narrower contract a future second backend must implement
- **Evidence**: Whole-src grep for `pane_exists`: the only production call site is TmuxMultiplexer.wait_for_pane_gone's internal lambda (tmux.py:378); no CLI/broker/monitor code calls `.pane_exists` on a `Multiplexer`-typed object (cli/member.py uses `wait_for_pane_gone`, `send_exit`, `kill_pane`, `capture_pane`, etc.; monitor/loop.py:81 uses `list_pane_ids`). Tests reference it only as a concrete-class monkeypatch target (tests/cli/test_member_delete.py:72, tests/multiplexer/test_tmux.py:443-576), which does not go through the Protocol.
- **Proposed change**: Remove the `pane_exists` declaration (and its docstring) from the `Multiplexer` Protocol in base.py, keeping the concrete `TmuxMultiplexer.pane_exists` method as an implementation detail that `wait_for_pane_gone` and the tests continue to use.
- **Why behavior-preserving**: Protocols are structural: `isinstance(TmuxMultiplexer(), Multiplexer)` (tests/multiplexer/test_protocol.py:17-19) still passes after removing a member, and since no code calls `pane_exists` via a `Multiplexer`-typed reference, type checking and runtime behavior are unchanged; the concrete method and all monkeypatch points stay.
- **Verifier notes**: The code-level facts check out: the sole production call is TmuxMultiplexer.wait_for_pane_gone's closure (tmux.py:378); cli/member.py calls only wait_for_pane_gone/send_exit/kill_pane/capture_pane/send_bash_command/send_poll_trigger via MULTIPLEXERS['tmux']; monitor/loop.py:80-81 uses a concrete TmuxMultiplexer with list_pane_ids; tests monkeypatch the concrete class (test_member_delete.py:72, test_tmux.py:443-576), and runtime_checkable isinstance still passes after removing a member. However the change is not strictly contract-preserving: SPEC.md §6.5 (line 1738) lists pane_exists in the Multiplexer 'Method surface' whose scope line covers 'the Multiplexer interface', and the section explicitly carves out only select_layout as 'internal to the tmux backend (not on the interface)' — the natural reading places pane_exists on the documented interface, so the removal creates SPEC drift (a documentation-complete blocker per .claude/rules/documentation-maintenance.md) unless SPEC.md is edited in the same change, and that edit is itself a reimplementation-contract narrowing, which the finding even advertises ('narrower contract a future second backend must implement'). The project demonstrably treats Protocol membership as a tested contract (tests/multiplexer/test_protocol.py:38-45 assert specific members are declared on the Protocol). A contract-surface change with a required doc edit is not a behavior-preserving simplification as scoped; refuted on the 'strictly behavior-preserving / documented contracts' criterion.

## Area: skills-cafleet (8/8 confirmed)

### [skills-cafleet-1] Director re-engagement paragraph stated twice near-verbatim within supervision.md
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:19, 33`
- **Estimated impact**: -6 to -8 lines in supervision.md
- **Evidence**: Line 19 (§ Communication Model, 'Facilitation cue'): 'The monitor loop does **not** wake the Director… The Director is re-engaged on demand: by the monitoring member's idle nudge (`cafleet member nudge`, which persists an ACKable broker task **and** fires the hardened, `Esc`-safeguarded inline preview…), and by the broker's inline-preview keystroke on every inbound `cafleet message send`. **Treat each such re-engagement as the cue to run the entire 5-step facilitation loop** (poll → ACK → dispatch → health-check → escalate)…'. Line 33 (§ The monitor heartbeat): '**The Director is never woken by the loop.** It is re-engaged only on demand: by the monitoring member's idle nudge (`cafleet member nudge`, which persists an ACKable broker task **and** fires the hardened, `Esc`-safeguarded inline preview…), and by the broker's inline-preview keystroke on every inbound `cafleet message send`. When woken by one of those, the Director runs the entire 5-step facilitation loop (poll → ACK → dispatch → health-check → escalate), not just an inbox read.' The two paragraphs carry the same three facts (loop never wakes Director; the two re-engagement channels; re-engagement = run the full 5-step loop) in nearly identical wording.
- **Proposed change**: Keep one full statement (the § heartbeat one at line 33, where the mechanism is defined) and reduce the line-19 'Facilitation cue' paragraph to its one load-bearing sentence plus a pointer ('see § The monitor heartbeat'), or vice versa.
- **Why behavior-preserving**: Docs-only; both paragraphs are in the same file read as a unit by the same audience (Directors), so no instruction is lost — one statement plus an in-page pointer preserves identical semantics.
- **Verifier notes**: Both quotes verified verbatim at supervision.md:19 and :33; they carry the same three facts. Same-file consolidation is safe: supervision.md's full-file readers are the Director (roles/director.md row #2) and the monitoring member (roles/monitor.md row #2), and no living doc links the 'Facilitation cue' anchor (only historical design-docs). Caveat: each copy has a unique clause that must survive — line 19's parenthetical (loop wakes only the monitoring member, firing when a watched agent is due) and its bolded load-bearing 'run the entire 5-step loop, NOT read-and-stop' sentence; line 33's closing 'the monitor decides *when*; this file defines *what*' sentence.

### [skills-cafleet-2] Identity-substitution (str.format) protocol stated in full in three places
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/director.md:102, 137`
- **Estimated impact**: -8 to -12 lines in director.md
- **Evidence**: SKILL.md:80-89 (§ Spawned-member identity via str.format substitution) is the canonical statement: the four placeholders `{fleet_id}` / `{agent_id}` / `{director_agent_id}` / `{coding_agent}`, 'Any literal brace in prompt text must be doubled (`{{` / `}}`)', unknown placeholder → UsageError exit 2, 'No identity environment variable is injected into the pane'. director.md:102 ('**Identity substitution (str.format)**') restates every one of those facts in one paragraph, adding only 'listing the four supported names' and 'with the just-registered agent rolled back'. director.md:137 (the two-stage rendering paragraph) enumerates the four placeholders a third time and repeats the brace-doubling rule ('double any literal brace as {{ / }}').
- **Proposed change**: Make SKILL.md § Spawned-member identity the owner. Collapse director.md:102 to a pointer plus its two deltas (rollback on error; the UsageError lists the four names), and drop the repeated placeholder enumeration/brace rule from director.md:137 (it already links context via the skeleton).
- **Why behavior-preserving**: SKILL.md is required reading for every agent including Directors (director.md is reached only via the skill), so consolidating to the SKILL.md statement plus deltas keeps every fact reachable in the same reading path.
- **Verifier notes**: All three statements verified (SKILL.md:80-89, director.md:102, director.md:137) and match the implementation (cli/_text_input.py substitute_spawn_placeholders). SKILL.md is the dispatch surface every reader loads before director.md, so consolidation keeps facts reachable. Scope correction: director.md:102 carries THREE deltas over SKILL.md, not two — (a) unknown-placeholder UsageError lists the four names, (b) malformed-brace raises the distinct 'double literal braces' UsageError (SKILL.md merges both error cases into one sentence), (c) just-registered-agent rollback. All three must survive the collapse. At :137, dropping the enumeration is safe only if 'the four identity placeholders' keeps an antecedent (the skeleton at :125-129 still shows them literally, and the '{agent_id} only-the-CLI-can-fill' point is already in SKILL.md:85).

### [skills-cafleet-3] 'Long or multi-line bodies MUST use --text-file' rule duplicated verbatim across two Director pages
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:17 (supervision.md), 106 (director.md)`
- **Estimated impact**: -3 lines and one fewer place to keep in sync
- **Evidence**: supervision.md:17: '**Long or multi-line bodies.** `message send` / `message broadcast` / `member nudge` accept a `--text-file <path>` (or `--text-file -` for stdin)… A long or multi-line body MUST be passed via `--text-file`, never inline `--text`, so it never lands on the command line and hits the shell's `ARG_MAX` limit. Short one-line bodies stay fine inline with `--text`.' director.md:106: '**Long or multi-line message bodies**: the same `ARG_MAX` cliff applies to `message send` / `message broadcast` / `member nudge`. A long or multi-line body MUST be passed via `--text-file <path>` (or `--text-file -` to read from stdin), never inline `--text`, so it never lands on the command line. Short one-line bodies stay fine inline with `--text`.' Same rule, same three commands, same closing sentence.
- **Proposed change**: Keep the director.md statement (it sits next to the related spawn-prompt ARG_MAX paragraph at line 104) and replace supervision.md:17 with a one-line pointer to director.md § Member Create, or the reverse — one page owns, the other links.
- **Why behavior-preserving**: Both pages are load-bearing required reading for the same Director audience (roles/director.md rows #2 and #3), so a link preserves the identical instruction with one authoritative wording.
- **Verifier notes**: Verified: supervision.md:17 and director.md:106 are the only two copies of the MUST rule repo-wide (SKILL.md:124 has a softer inline mention for send). Scope correction on direction: supervision.md's audience is NOT only the Director — the monitoring member is required to read it (roles/monitor.md row #2) and is the primary `member nudge` sender, while director.md:5 says members do not need that file. Prefer the finding's 'reverse' option (supervision.md owns; director.md § Member Create points), which keeps the rule in every reader's required path; pointing supervision.md at director.md still leaves the rule reachable but routes the monitor into a page marked not-for-members.

### [skills-cafleet-4] broadcast.md flag table contradicts the actual CLI and sibling pages: --text marked required, --text-file omitted
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `skills/cafleet/reference/broadcast.md:10-13`
- **Estimated impact**: 1 table row corrected; removes an internal contradiction that misdirects long-body broadcasts to ARG_MAX failures
- **Evidence**: broadcast.md's flag table lists only '| `--text <body>` | yes | Message body fanned out…|'. The implementation (cafleet/src/cafleet/cli/message.py:57-80) defines both `--text` (default None) and `--text-file` with `read_text_input(text, text_file)` enforcing exactly-one-of. Sibling pages state the contract correctly: supervision.md:17 '`message send` / `message broadcast` / `member nudge` accept a `--text-file <path>` (or `--text-file -` for stdin) alternative to inline `--text`' and director.md:106 says the same. broadcast.md is the page an agent reads before broadcasting (SKILL.md load-bearing-on-trigger row), yet it presents `--text` as unconditionally required and hides `--text-file`.
- **Proposed change**: Correct the table row to the exactly-one-of `--text` / `--text-file` contract (mirroring the wording already used for `message send` in SKILL.md:124), so the page matches the CLI it documents.
- **Why behavior-preserving**: Documentation accuracy fix only — it describes the CLI's existing behavior; no code, flag, or output changes.
- **Verifier notes**: Fully verified. broadcast.md:12 marks `--text` required=yes and has no `--text-file` row. cli/message.py:57-85 defines both flags with read_text_input (cli/_text_input.py) enforcing exactly-one-of; docs/spec/cli-options.md § message broadcast documents both flags with 'Required: no / exactly one of the two'; supervision.md:17 and director.md:106 both state broadcast accepts --text-file. broadcast.md is the load-bearing-on-trigger page before a broadcast (SKILL.md:33). Genuine internal contradiction; the proposed table correction is a docs-only accuracy fix describing existing CLI behavior.

### [skills-cafleet-5] Poll semantics restated within supervision.md (facilitation step 1 vs Stall Response Stage 1) on top of SKILL.md § Poll
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:113, 143`
- **Estimated impact**: -2 to -3 lines
- **Evidence**: supervision.md:113 (facilitation step 1): '`cafleet message poll …` returns only the un-acked (`input_required`) deliveries; ACKing each one (step 2) consumes it, so the next tick's poll surfaces only what has arrived since.' supervision.md:143 (Stage 1): '`cafleet message poll` returns only the un-acked (`input_required`) deliveries addressed to the Director, newest first. ACKing a delivery consumes it, so a later poll surfaces only what has arrived since the last ACK — there is no last-tick timestamp to track.' Both restate SKILL.md:128 (§ Poll): 'Returns only un-acked (`input_required`) deliveries addressed to this agent, newest first; ACKing one drops it from `poll` output.'
- **Proposed change**: Keep the semantics once in supervision.md (Stage 1, which carries the extra 'no last-tick timestamp to track' nuance) and trim facilitation step 1 to the command plus a back-reference; SKILL.md § Poll stays canonical for the command itself.
- **Why behavior-preserving**: Same page, same reader; consolidating identical semantics to one in-page statement plus a reference loses no instruction.
- **Verifier notes**: All three quotes verified verbatim (supervision.md:113, :143; SKILL.md:128). Same-file, same-reader consolidation with Stage 1 as owner is safe; Stage 1 uniquely carries 'newest first' and 'no last-tick timestamp to track'. Caveat: step 1's trimmed form must keep the '(step 2)' linkage coherent — the ACK-consumes fact motivates step 2 of the loop — so keep the command plus a short back-reference as proposed.

### [skills-cafleet-6] Two-nudge escalation rule stated twice within supervision.md
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:117, 158-160`
- **Estimated impact**: -1 to -2 lines; removes a drift risk between two statements of the same threshold
- **Evidence**: Step 5 of the facilitation loop (line 117): '**Escalate** to the user via {decision_surface} after two nudges produce no progress, or whenever a queued action requires a *new* user decision…'. § Escalation (line 160): 'If a member is still unresponsive after 2 nudges via `cafleet message send` AND `cafleet member capture` shows no forward progress in the terminal buffer, escalate to the user via {decision_surface} … with concrete options (e.g. re-nudge once more / re-spawn the member / drop its task).' Both encode the same '2 nudges → escalate via {decision_surface}' threshold in the same file; § Escalation adds the capture-corroboration condition and the options list.
- **Proposed change**: Make § Escalation (under Stall Response) the owner of the threshold and its conditions; step 5 keeps only the 'new user decision' trigger plus a pointer to § Escalation for the stall path.
- **Why behavior-preserving**: Within-page consolidation; the fuller § Escalation statement subsumes step 5's stall clause, so the identical threshold and conditions remain in one authoritative spot.
- **Verifier notes**: Both quotes verified (supervision.md:117 and :160 under § Escalation at :158). Same threshold ('2 nudges → escalate via {decision_surface}') in the same file; § Escalation's statement is strictly fuller (adds the capture-corroboration AND-condition and the concrete options list), so it subsumes step 5's stall clause. Caveat: step 5 must retain its two non-duplicated parts — the 'new user decision' trigger and the 'no passive-hold messages' prohibition.

### [skills-cafleet-7] Shutdown ordering summarized three times in supervision.md, all deferring to recovery.md as canonical
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:127-129, 179-181`
- **Estimated impact**: -2 to -3 lines; ordering maintained in one place instead of four
- **Evidence**: Monitor Lifecycle 'Teardown (first-out)' row (127): 'Stop the monitor's background task FIRST, then delete the monitoring member before ordinary members. The authoritative full ordering is [recovery.md] § *Shutdown Protocol*.' Lifecycle rule (129): '…at teardown the monitor is stopped FIRST (first-out)… The full stop mechanism + ordering is canonical in [recovery.md] § *Shutdown Protocol*.' § Cleanup Protocol (181): 'Cleanup follows [recovery.md] § Shutdown Protocol (first-out): stop the monitor's `monitor start` task FIRST → `cafleet member delete` the monitoring member → each ordinary member → verify the roster is empty → `cafleet fleet delete…` → `cafleet fleet list`. The full stop mechanism … and the race rationale are canonical there.' — a compressed restatement of recovery.md:43-51's five steps, in a page that has already pointed there twice.
- **Proposed change**: Collapse § Cleanup Protocol to the single pointer sentence ('Cleanup follows recovery.md § Shutdown Protocol') and keep the first-out fact once (in the Lifecycle table row with its race rationale); recovery.md:43-51 remains the sole full ordering.
- **Why behavior-preserving**: recovery.md is already declared the authoritative ordering by all three passages, and it is load-bearing-on-trigger reading before any teardown (roles/director.md:24), so removing the third summary loses no reachable instruction.
- **Verifier notes**: All three passages verified (supervision.md:127, :129, :181; plus the Quick Reference row at :197 as the fourth, consistent with the finding's 'one place instead of four'). recovery.md:43-51 verified as the canonical 5-step Shutdown Protocol, including the no-monitor-stop / message-the-monitoring-member mechanism restated at :181. recovery.md is gated pre-teardown reading for the Director (SKILL.md load-bearing-on-trigger row and roles/director.md:24), so collapsing § Cleanup Protocol to the pointer loses nothing reachable. Caveat: keep the § Cleanup Protocol heading itself (SKILL.md:97 advertises 'Cleanup' among supervision.md's topics) and keep the first-out fact + race rationale once in the Lifecycle table/rule.

### [skills-cafleet-8] Monitor-first spawn + 'Director never runs monitor start' gate restated near-verbatim in SKILL.md and repeatedly inside supervision.md
- **Status**: CONFIRMED
- **Kind**: doc-redundancy
- **Location**: `skills/cafleet/reference/supervision.md:31, 46, 83, 89, 95 (supervision.md); 95 (SKILL.md)`
- **Estimated impact**: -4 to -6 lines across supervision.md/SKILL.md
- **Evidence**: SKILL.md:95: 'the **FIRST** member created is the dedicated monitoring member (`cafleet member create --role monitor --model {monitor_model} --text-file <rendered monitor prompt>`). It owns the heartbeat and gates every ordinary `member create` behind its `ready: monitor live` handshake. The Director never runs `cafleet monitor start` itself.' supervision.md:83 repeats it almost verbatim: 'The **first** `cafleet member create` in the fleet IS the dedicated monitoring member (`--role monitor --model {monitor_model}`); it starts the monitor and gates every ordinary `member create` behind its `ready: monitor live` handshake. The Director never runs `cafleet monitor start` itself.' Within supervision.md the 'Director does not run the monitor' fact also appears at line 31 ('**The monitoring member — not the Director — runs `cafleet monitor start`**'), line 46 ('It is the **one** process that runs `cafleet monitor start` — the Director no longer runs the monitor itself'), and line 95 ('Never spawn ordinary members before the monitoring member's `ready: monitor live` handshake').
- **Proposed change**: Within supervision.md, keep the full statement once in § Spawn Protocol (line 83, where the gate is operationalized) and reduce the line-31 and line-46 restatements to short forward references; SKILL.md:95 can stay as the dispatch-level summary or be trimmed to its pointer sentence at line 97.
- **Why behavior-preserving**: All five statements carry the identical rule for the same Director reader; one full in-page statement plus references preserves the instruction while eliminating four copies that must be edited in lockstep.
- **Verifier notes**: All cited lines verified (supervision.md:31, :46, :83, :89, :95; SKILL.md:95). Consolidation to one full statement at § Spawn Protocol (:83) with forward references is behavior-preserving. Scope corrections: (1) lines 31 and 46 are single sentences embedded in otherwise-unique paragraphs (line 31 is already half a pointer), so realistic savings are below the estimated -4 to -6 lines; (2) line 95 conflates a different fact (the ready-handshake spawn gate, restating :83's own section) rather than the 'Director never runs monitor start' fact; (3) prefer the finding's keep-SKILL.md:95-as-is option — it is the dispatch-level Team-supervision summary that the skill frontmatter ('requires the dedicated monitoring member') and roles/director.md:16 lean on; trimming it to the :97 pointer is reachability-safe but weakens the only pre-supervision.md statement of the gate.

## Area: skills-workflows (4/8 confirmed)

### [skills-workflows-1] Stale trailing `$ARGUMENTS` interpolation token in 3 of 5 workflow bodies
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `skills/cafleet-design-doc/interview/interview.md:255`
- **Estimated impact**: -3 lines; removes a confusing no-op token from 3 contract pages
- **Evidence**: interview.md ends with a bare `$ARGUMENTS` line (255); so do skills/cafleet-research/report/report.md:284 and skills/cafleet-research/presentation/presentation.md:305. The other two workflow bodies (create/create.md, execute/execute.md) have no such trailing line — they only reference `$ARGUMENTS` in prose (e.g. create.md:64 'canonicalize `$ARGUMENTS`'), which interview/report/presentation also do. The trailing bare token is a leftover from the slash-command era: these files are now sub-pages read via the Read tool from the SKILL.md dispatcher, where no command-style interpolation occurs.
- **Proposed change**: Delete the trailing `$ARGUMENTS` line from interview.md:255, report.md:284, and presentation.md:305.
- **Why behavior-preserving**: The token is never interpolated in a Read-loaded sub-page; two of the five workflow bodies already omit it and function identically. All prose references to `$ARGUMENTS` (the actual instructions) are untouched.
- **Verifier notes**: Verified: interview.md ends at line 255 with a bare `$ARGUMENTS` (file is 255 lines), report.md:284 and presentation.md:305 likewise; create.md and execute.md have no trailing token and reference `$ARGUMENTS` only in prose, as the other three also do. The workflow bodies are sub-pages loaded via Read from the SKILL.md dispatchers (no .claude/commands/ registration anywhere, including ~/.claude; codex/opencode overlays load skills by reading files, so no backend interpolates them either). Repo-wide grep (skills/, docs/, SPEC.md, README.md, .claude/, Python source/tests) finds no reference to the trailing line; design docs 0000088/0000093's 'keep $ARGUMENTS' instructions applied to the pre-merge standalone SKILL.md files where interpolation actually occurred. Deletion is behavior-preserving.

### [skills-workflows-2] Member 'Shutdown' section duplicated near-verbatim across 12 role files with no canonical home
- **Status**: REFUTED/RESCOPED
- **Kind**: duplication
- **Location**: `skills/cafleet-design-doc/create/roles/drafter.md:83-85`
- **Estimated impact**: ~-30 lines across 12 files; one canonical home instead of two drifting variants
- **Evidence**: The paragraph 'The Director terminates you via `cafleet member delete` ... (sends the backend exit keystroke, waits up to 15 s) ... If the Director instead messages you to wrap up first, send one final report...' is word-identical in drafter.md:83-85, create/roles/reviewer.md:68-70, execute/roles/programmer.md:114-116, tester.md:85-87, verifier.md:80-82, and near-identical in execute/roles/reviewer.md:72-74 ('your process' vs 'your `claude` process') and interview/roles/analyzer.md:88-90. A second identical variant ('...no message-level handshake is required.') repeats in cafleet-research report/roles/manager.md:108-110, researcher.md:77-79, scout.md:87-89, presentation/roles/presentation.md:100-102, transcript.md:84-86. The delete mechanics are already canonical Director-side in skills/cafleet/reference/director.md:171 (§ Member Delete), but no member-side page owns the paragraph.
- **Proposed change**: Add the member-shutdown paragraph once to the member-side anchor (skills/cafleet/roles/member.md, which every spawned member already reads) and replace the 11 duplicate sections with a one-line '## Shutdown — see the cafleet skill's roles/member.md § Shutdown' pointer. Keep only genuinely role-specific shutdown text (the Visual Reviewer's CLOSE handshake in visual-reviewer.md:150-155).
- **Why behavior-preserving**: The exact same instruction text is preserved at a single canonical location that all members already load; only the restatements are replaced with links, matching the pattern the same role files already use for poll/ack command shapes ('command shapes in the `cafleet` skill core').
- **Verifier notes**: The duplication itself is real (verified both variants across the 12 cited files, and that no member-side page owns the paragraph). But the load-bearing premise of the proposed change is false: skills/cafleet/roles/member.md is NOT 'read by every spawned member'. Workflow members' Required-reading gates are overlay + base-dir.md + coordination.md plus their own workflow roles/<role>.md (verified in drafter.md, reviewer.md, etc.), the canonical spawn-prompt skeleton (reference/director.md:110-137) never points members at roles/member.md, and cafleet/SKILL.md does not route ordinary members there — roles/member.md is only the anchor for generic/ad-hoc members (referenced from exec-routing.md and supervision.md). Also 'word-identical' overstates: interview/roles/analyzer.md:90 omits the entire wrap-up-first sentence, and execute/roles/reviewer.md:74 deliberately says backend-neutral 'your process' (the execute Reviewer spawns on {reviewer_model}, which can be a non-claude backend) vs 'your `claude` process' elsewhere. A single canonical paragraph would either drop these deltas or wrongly generalize them, so the change as proposed is not strictly behavior-preserving.

### [skills-workflows-3] Two design-doc Director role files duplicate three whole sections verbatim
- **Status**: REFUTED/RESCOPED
- **Kind**: duplication
- **Location**: `skills/cafleet-design-doc/create/roles/director.md:33-38, 79-81, 83-85`
- **Estimated impact**: ~-20 lines; eliminates a 3-section drift surface between the two Director docs
- **Evidence**: create/roles/director.md and execute/roles/director.md carry word-identical text for: (1) 'Idle Semantics & Stall Response' intro + both 'skill-specific' rungs ('Do NOT skip rungs' / 'Escalation is user-facing') — create:33-38 vs execute:45-50; (2) 'User delegation for a member's relayed question' — create:79-81 vs execute:116-118, which itself ends by naming its canonical ('the neutral pointer is the cafleet skill's `reference/director.md` § Answering a member's relayed question'); (3) 'Routing member bash requests' — create:83-85 vs execute:120-122, identical except the role list, and ending 'Full invocation + flag layout in the `cafleet` skill § Routing Bash via the Director'. The rungs labeled 'NOT in those skills and stay here' are in fact identical in both files, i.e. not workflow-specific.
- **Proposed change**: Move the three shared sections into one family reference page (e.g. skills/cafleet-design-doc/reference/ alongside coordination.md, which both Director role files already Read per their Required-reading tables) and replace each restatement with a one-line pointer; or at minimum reduce (2) and (3) to the one-line pointers to the cafleet-skill sections they already cite.
- **Why behavior-preserving**: The instruction text moves unchanged to a page both Directors are already gated to read (same mechanism as reference/coordination.md); no verb, command, or escalation rule changes.
- **Verifier notes**: The factual duplication checks out: sections (1) create:33-38 vs execute:45-50, (2) create:79-81 vs execute:116-118, and (3) create:83-85 vs execute:120-122 are word-identical except the role list in (3). But the proposed reductions are not behavior-preserving. For (2), the cited 'canonical' (cafleet reference/director.md § Answering a member's relayed question, line 198-200) documents a DIFFERENT scenario — the ordinary message-relayed question, answered 'as an ordinary cafleet message send ... not a pane keystroke' — whereas the role-file section governs a member paused on a decision-prompt pane frame, mandating the overlay's decision-relay primitive via the Director's Bash tool and 'Never print a fenced bash block'; that content exists ONLY in these two role files (grep-confirmed), so a one-line pointer to director.md deletes contract. For the move-to-family-reference variant: the Directors are gated on coordination.md, not on any new page 'alongside' it, and both files explicitly state these rungs 'are NOT in those skills and stay here' — the reduction converts gated inline instructions into at-need pointers, weakening the read guarantee. (3)'s named canonical does exist: 'Routing Bash via the Director' is exec-routing.md's title and carries the serialization rule, but not the per-workflow role list / {permission_flags} framing.

### [skills-workflows-4] Step-0 `$ARGUMENTS` canonicalization bullets duplicated verbatim across three workflow bodies
- **Status**: REFUTED/RESCOPED
- **Kind**: duplication
- **Location**: `skills/cafleet-design-doc/create/create.md:62-68`
- **Estimated impact**: ~-25 lines net; a single copy to keep in sync with the cafleet base-dir consumer contract
- **Evidence**: The 'Relative input — accept any of: `0000060-foo`, `0000060-foo/design-doc.md`, `design-docs/0000060-foo`, ... Canonicalize to `design-docs/<slug>` by: (1) stripping the trailing `/design-doc.md` ... (3) prepending `design-docs/`...' bullet and the 'Absolute path ... Step 0 accepts only the task-folder path, not a child file...' bullet are word-identical in create.md:64-66, interview/interview.md:79-81, and execute/execute.md:75-77 (execute differs only in the example slug). Each is followed by a near-identical 'Branch on Step 0's outcome' paragraph (create:68, interview:83, execute:79) with only the output-variable names differing.
- **Proposed change**: Factor the shared canonicalization procedure (relative-input rules, absolute-path rule, `<unset>` branch skeleton) into one page under skills/cafleet-design-doc/reference/ and have each workflow's Step 0/1 state only its delta (which variables it sets: `${DOC_PATH}` / `dir_path`+`doc_path` / `${RESOLVED_ARGS}`).
- **Why behavior-preserving**: The canonicalization algorithm is character-identical across the three copies; extracting it to a single linked page changes no rule, and the per-workflow variable bindings stay in each body.
- **Verifier notes**: Partially accurate: the Relative-input and Absolute-path bullets are character-identical across create.md:64-66, interview.md:79-81, execute.md:75-77 (only CREATE's absolute-path example slug differs, not execute's as claimed). But the claim that the 'Branch on Step 0's outcome' paragraphs differ 'only in output-variable names' is false and breaks the proposal: on the <unset> branch, execute.md:79 sets ${RESOLVED_ARGS} to the LITERAL $ARGUMENTS path (so Tier 1/2 validation runs against the user-supplied path), while create.md:68 and interview.md:83 use the CANONICALIZED absolute path with /design-doc.md appended — semantically different behaviors, so a shared '<unset> branch skeleton' cannot be factored without changing execute's contract or keeping the whole branch per-workflow anyway. Additionally, the canonicalization procedure already has a declared canonical home — skills/cafleet/reference/base-dir.md § Consumer contract (line 28, the cafleet-design-doc row states the identical 3-step rule and absolute-path exception) — so a new page under cafleet-design-doc/reference/ would add a third statement of the rule rather than deduplicate to the existing canonical.

### [skills-workflows-5] Monitoring-member spawn block restated near-verbatim in four workflow bodies whose canonical already lives in the cafleet skill
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `skills/cafleet-research/report/report.md:83-97`
- **Estimated impact**: ~-40 lines across 4 workflow bodies
- **Evidence**: The sentence 'Render the canonical monitoring-member spawn prompt (the **conditional** idle-nudge routine — re-engage the Director via `cafleet member nudge` only when un-acked inbox items or stalled members can be named)...' is verbatim in create/create.md:104, interview/interview.md:115, report/report.md:87, presentation/presentation.md:104 (grep-confirmed), each followed by an identical `cafleet --json member create ... --role monitor --model {monitor_model} --text-file ${BASE}/prompts/monitor-<UTC-compact>.md` code block and an identical 'See the `cafleet` skill's `roles/monitor.md` for the canonical spawn prompt and lifecycle' pointer. The first-in gate paragraph above each block is also near-identical. skills/cafleet/roles/monitor.md and reference/supervision.md are the declared canonical owners.
- **Proposed change**: Keep each workflow body's gate sentence ('first `member create` is the monitor; its `ready: monitor live` handshake gates the ordinary spawns') and replace the repeated render-instructions + spawn code block with a pointer to the cafleet skill's roles/monitor.md § canonical spawn prompt (which already contains the command shape).
- **Why behavior-preserving**: The workflow bodies themselves declare roles/monitor.md canonical; the command and prompt are identical in all copies, so replacing restated text with the link the pages already carry changes no procedure.
- **Verifier notes**: Verified: the render sentence's lead is verbatim in create.md:104, interview.md:115, report.md:87, presentation.md:104 (tails differ only in the audit-pattern step reference), each followed by an identical monitor `cafleet --json member create` block (bracket style <> vs [] differs between the two skill families) and a 'See the cafleet skill's roles/monitor.md for the canonical spawn prompt and lifecycle' pointer. Crucially, the fifth body already proves the reduction safe: execute.md:167 (Step 3b) spawns the same monitor with ONLY the gate paragraph + the roles/monitor.md pointer and no code block. Two corrections to the evidence: roles/monitor.md § Canonical spawn prompt carries the per-role delta (--role monitor --model {monitor_model}, omit --coding-agent) and defers the full command shape to reference/director.md's Member Create examples (lines 20-23) and each overlay's Worked resolution, rather than 'already containing the command shape' itself; and the proposal should retain each body's teardown tail sentence (first-out step reference) alongside the gate sentence. With those noted, the change preserves behavior.

### [skills-workflows-6] Spawn-prompt audit-file blockquote duplicated across five workflow bodies, each already naming its canonical
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `skills/cafleet-design-doc/execute/execute.md:193`
- **Estimated impact**: ~-25 lines across 5 workflow bodies; one contract text instead of three drifting variants
- **Evidence**: The '**Spawn-prompt audit file (two-step pattern)**: every spawn in this skill follows the same two steps — (1) **render** ... (2) **write** it to `${BASE}/prompts/<role>-<UTC-compact>.md` (`<UTC-compact>` = `datetime.now(UTC).strftime(...)`; ... same-second collision → append `_2`, `_3`...) ... The pre-spawn file IS both the CLI input AND the permanent audit artifact...' blockquote is verbatim-identical in create/create.md:125 and execute/execute.md:193, with a slightly shortened copy at interview/interview.md:131; report/report.md:129 and presentation/presentation.md:124 share their own verbatim 'Spawn mechanics' variant of the same contract. Every copy ends by citing the canonical: 'See the `cafleet` skill's `reference/base-dir.md` § No-bypass write protocol and its `reference/director.md` § Member Create — Scratch and audit files for the contract'.
- **Proposed change**: Reduce each blockquote to the audit-file path template plus the existing pointer to base-dir.md § No-bypass write protocol / director.md § Member Create, letting the canonical pages carry the collision rule, the placeholder-pre-substitution note, and the `<unset>` guarded-skip branch (they already do).
- **Why behavior-preserving**: base-dir.md and director.md are Required reading row #2 for every Director running these bodies, so the contract is still read before any spawn; only the restatement is removed.
- **Verifier notes**: Verified: create.md:125 and execute.md:193 are verbatim-identical; interview.md:131 is a shortened copy (itself precedent that reduction works); report.md:129 and presentation.md:124 share a verbatim 'Spawn mechanics' variant. Every copy ends with the exact pointer cited. The canonical pages do already carry the contract: reference/director.md § Member Create — Scratch and audit files has the <UTC-compact> format and _2/_3 collision rule (line 160), the placeholders-pre-substitution note (line 161), and the ${BASE} == <unset> guarded-skip + inline --text fallback (line 163); base-dir.md § No-bypass write protocol names the ${BASE}/prompts/<role>-<UTC-compact>.md template and the <unset> hard-stop. One correction: reference/director.md is NOT 'Required reading row #2' (base-dir.md holds row #2 in every workflow body and Director role file; director.md is not in those gated tables) — but every spawn step links director.md's canonical spawn-prompt skeleton, on the same page as the audit-file contract, and the reduced blockquote keeps the pointer, so the contract is still read before any spawn. Behavior-preserving with that correction.

### [skills-workflows-7] Text-overflow remediation priority order contradicts itself across four presentation pages
- **Status**: REFUTED/RESCOPED
- **Kind**: stale-reference
- **Location**: `skills/cafleet-research/reference/slidev/techniques/formatting.md:118`
- **Estimated impact**: ~-6 lines; removes a 4-way contradiction on a quality-gate rule
- **Evidence**: formatting.md:118 says '**Priority order when text overflows**: (1) shorten text; (2) `fontSize` prop; (3) split into multiple slides (last resort).' But reference/slidev.md:57 says 'Do NOT shorten text just to avoid wrapping — that loses information. Instead: use non-breaking characters ..., adjust `fontSize`, or restructure the layout.' And the acting role, presentation/roles/presentation.md:62-76, prescribes '**Fix structurally first, font last**' (1 refactor into sub-bullets → 2 split slides → 3 non-breaking chars → 4 `fontSize` as 'secondary lever only' → 5 minor rephrasing, 'do NOT shorten to the point of losing information'). visual-reviewer.md:72's remediation hint leads with '`fontSize` prop to shrink text until it fits ... or shortening text', contradicting both.
- **Proposed change**: Keep the Presentation Specialist's structural-first procedure (presentation.md:62-76) as the single remediation order; replace formatting.md:118's conflicting 3-item list and visual-reviewer.md:72's remediation hint with a pointer to it (the VR hint is doubly redundant — visual-reviewer.md:84 already states 'Remediation is the Presentation Specialist's job — you detect and flag').
- **Why behavior-preserving**: Only the Presentation Specialist performs remediation, and its role file already carries the authoritative order; deleting the contradicting advisory restatements on pages whose readers do not remediate (VR) or that defer to the role (formatting.md is a component reference) leaves the enacted procedure unchanged.
- **Verifier notes**: The 4-way tension is factually verified: formatting.md:118 says shorten-first/split-last; slidev.md:57 forbids shortening to avoid wrapping; presentation.md:62-76 mandates structural-first with fontSize as 'secondary lever'; visual-reviewer.md:72 leads with fontSize 'or shortening text', and VR:84 does say remediation is the Specialist's job. But the proposed change is not behavior-preserving, and the premise 'only the Presentation Specialist performs remediation' is false: cafleet-research/SKILL.md dispatches reference/slidev.md (and its techniques/ pages) as STANDALONE on-demand utilities ('you are authoring slides with the custom Slidev theme ... no team needed'), so a main-session slide author consumes formatting.md/slidev.md without ever reading presentation/roles/presentation.md — for that reader, replacing formatting.md's shorten-first order with the role file's structural-first order is a substantive contract change, not a no-op. Resolving a genuine contradiction necessarily eliminates one of the two currently-documented behaviors; this is a worthwhile consistency FIX, but it is behavior-relevant, and pointing a standalone reference page into a team role file also inverts the repo's reference<-role layering (presentation.md:56 points at formatting.md, not vice versa).

### [skills-workflows-8] Create-workflow Reviewer role retains pre-schema phrasing claiming findings travel in cafleet messages
- **Status**: CONFIRMED
- **Kind**: stale-reference
- **Location**: `skills/cafleet-design-doc/create/roles/reviewer.md:3, 62-66`
- **Estimated impact**: 2 sentences; removes an internal contradiction inside one role file and against the canonical coordination reference
- **Evidence**: reviewer.md:3 says the Reviewer 'provide[s] specific, actionable feedback via `cafleet message send`', and §Iterative Improvement Loop (line 64) says 'Your reviews are sent to the Director, who forwards them to the Drafter.' Both contradict the canonical protocol the same file states at line 36 ('findings in inline `COMMENT(reviewer): [TAG] <body>` markers at the affected section (never in the cafleet body)'), reference/coordination.md:9 ('`cafleet message send` is a poke, not a payload'), create/create.md:213 ('the Director does NOT relay the finding text in cafleet'), and drafter.md:24 ('read the standing `COMMENT(reviewer)` markers in the design doc — that is where the Reviewer's findings live').
- **Proposed change**: Rephrase reviewer.md:3 to 'provide specific, actionable feedback as inline `COMMENT(reviewer)` markers, signalled via `cafleet message send`', and rewrite the first sentence of §Iterative Improvement Loop to the marker-based flow ('the Director routes the Drafter to your standing markers with `ready (doc)`'), matching the execute-workflow reviewer's already-correct §Review-and-Revise Loop (execute/roles/reviewer.md:66-70).
- **Why behavior-preserving**: The canonical verb+pointer+marker protocol (coordination.md, and line 36 of this same file) is unchanged; the edit removes leftover pre-schema wording that contradicts it, so agents following the corrected text do exactly what the rest of the skill already mandates.
- **Verifier notes**: All cited passages verified verbatim: reviewer.md:3 ('provide specific, actionable feedback via `cafleet message send`'), §Iterative Improvement Loop line 64 ('Your reviews are sent to the Director, who forwards them to the Drafter'), the same file's line 36 ('findings in inline COMMENT(reviewer): [TAG] <body> markers at the affected section (never in the cafleet body)'), coordination.md:9 ('`cafleet message send` is a poke, not a payload'), create.md:213 ('the Director does NOT relay the finding text in cafleet'), drafter.md:24 ('read the standing COMMENT(reviewer) markers ... that is where the Reviewer's findings live'), and the execute reviewer's marker-based Review-and-Revise Loop at execute/roles/reviewer.md:66-70. The canonical marker protocol is unambiguous and gated as the Reviewer's Required-reading row 3, and every cross-referencing page agrees with it, so lines 3 and 64 are stale descriptive residue contradicting the operative contract; rewording them to match changes no enacted procedure. Note the fix is a rewrite of two sentences (a drift correction), not a pure deletion, and the second half of line 64 ('the Director then re-routes the updated draft to you via cafleet message send') is already correct and should be kept.

## Area: tests (8/8 confirmed)

### [tests-1] Dead helper _make_alembic_cfg while its exact body is inlined 5 times elsewhere
- **Status**: CONFIRMED
- **Kind**: dead-code
- **Location**: `cafleet/tests/_helpers.py:15-21`
- **Estimated impact**: -7 lines (delete) or ~-20 lines (factor 5 call sites)
- **Evidence**: grep for _make_alembic_cfg across the whole repo hits only its definition at cafleet/tests/_helpers.py:15 — zero call sites. Meanwhile the identical construction (importlib.resources.as_file(files("cafleet.db")/"alembic.ini") -> Config -> set_main_option("sqlalchemy.url", f"sqlite:///{db_path}")) is re-implemented inline 5 times: cafleet/tests/db/test_alembic_smoke.py:20-25, 72-76, 438-443 and cafleet/tests/broker/test_monitor.py:631-635, 708-712.
- **Proposed change**: Either delete _make_alembic_cfg (it is unused, and importlib.resources becomes a smaller import surface), or — better — import it at the 5 inline sites and delete the duplicated with-blocks. Note test_monitor.py:629's comment about holding the as_file context open: the helper exits the context before returning, matching the smoke-test usage where command.upgrade runs inside; if the context lifetime matters at a site, keep that site inline and just delete the dead helper.
- **Why behavior-preserving**: Deleting an unused private test helper changes nothing. Replacing inline duplicates with a helper that builds the identical Config object with the identical URL is a pure refactor of test setup; assertions are untouched.
- **Verifier notes**: Dead-code claim verified: grep hits only the definition at cafleet/tests/_helpers.py:15-21 plus design-doc mentions (the canonical historical record, which stays). All 5 cited inline sites exist and match. Scope correction: only the DELETE option is strictly behavior-preserving. The 'better' factoring option is not equivalent at ANY of the 5 sites, not just test_monitor.py — all five run alembic operations (command.upgrade at test_alembic_smoke.py:25/443-448, ScriptDirectory.from_config+walk_revisions at 76-77, command.upgrade/downgrade at test_monitor.py:638-649/716+) INSIDE the as_file context, whereas the helper exits the context before returning. The finding's parenthetical that the helper 'matches the smoke-test usage' is backwards. Under a zipped-wheel install the materialized alembic.ini is deleted at context exit (see the comment in cafleet/src/cafleet/db/init.py:31-34), so the factored form would depend on the editable-install accident. Delete the helper (and its then-unused importlib.resources / alembic.config.Config imports); keep the inline blocks.

### [tests-2] Seven pass-through _autouse_reset_engine fixtures in tests/cli are redundant with the autouse _cli_registry
- **Status**: CONFIRMED
- **Kind**: redundant-abstraction
- **Location**: `cafleet/tests/cli/conftest.py:9-24`
- **Estimated impact**: -28 lines across 7 files, one less fixture indirection per file
- **Evidence**: cafleet/tests/cli/conftest.py:10 defines the autouse fixture `_cli_registry(tmp_path, monkeypatch, _reset_engine_singletons)` which already requests _reset_engine_singletons for every test in tests/cli/. Yet 7 files re-add an identical 3-line pass-through: `@pytest.fixture(autouse=True)\ndef _autouse_reset_engine(_reset_engine_singletons): pass` — cafleet/tests/cli/test_fleet_flag.py:15-17, test_monitor.py:32-34, test_setup.py:123-125, test_doctor.py:30-32, test_skills_guard.py:37-39, test_fleet_bootstrap.py:18-20, test_fleet.py:15-17.
- **Proposed change**: Delete all seven _autouse_reset_engine fixtures. The conftest autouse _cli_registry already instantiates _reset_engine_singletons for every CLI test.
- **Why behavior-preserving**: pytest caches a fixture per test, so _reset_engine_singletons is instantiated exactly once either way, and it is already activated first via the conftest-level autouse fixture (conftest autouse fixtures run before module-level ones). Setup/teardown order and effects are identical after deletion.
- **Verifier notes**: Verified: all seven files (test_fleet_flag.py:15-17, test_monitor.py:32-34, test_setup.py:123-125, test_doctor.py:30-32, test_skills_guard.py:37-39, test_fleet_bootstrap.py:18-20, test_fleet.py:15-17) contain the identical @pytest.fixture(autouse=True) pass-through, all module-level, all under tests/cli/ where the autouse _cli_registry (tests/cli/conftest.py:9-24) already requests _reset_engine_singletons (tests/conftest.py:50-56, function-scoped). pytest caches one instance per test, so the pass-through adds nothing; setup runs before _cli_registry's redirect and teardown after it in both worlds, and the reset body (set singletons to None) is order-insensitive anyway. No other code, doc, or fixture references the name _autouse_reset_engine. Deletion is a no-op.

### [tests-3] broker.create_fleet test wrapper re-implemented in 4 files identical to tests/broker/_helpers._create_fleet
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/tests/broker/_helpers.py:7-14`
- **Estimated impact**: ~-25 lines across 4 files
- **Evidence**: tests/broker/_helpers.py:7 defines _create_fleet(label=None) calling broker.create_fleet(label, director_context=MultiplexerContext(session="main", window_id="@3", pane_id="%0"), coding_agent="claude"). Byte-equivalent local copies: cafleet/tests/broker/test_inline_preview.py:40-45 (_create_fleet), cafleet/tests/broker/test_administrator.py:14-20 (_FAKE_DIRECTOR_CTX + _create_fleet_with_ctx), cafleet/tests/webui/test_api_format.py:31-35 (_create_fleet), cafleet/tests/webui/test_monitor_api.py:51-55 (_create_fleet). create_fleet's signature (cafleet/src/cafleet/broker/fleets.py:16-21) defaults label to None, so omitting label (administrator/webui variants) is identical to the helper's label=None.
- **Proposed change**: Replace the four local functions with `from tests.broker._helpers import _create_fleet` (test_administrator.py keeps its name via `_create_fleet_with_ctx = _create_fleet` or direct call-site edits). tests/monitor/test_loop.py and tests/output/test_render_broadcast_summary.py already import it cross-directory, proving the import pattern is established.
- **Why behavior-preserving**: Each local copy constructs the same MultiplexerContext values and the same create_fleet call; swapping to the shared helper produces the identical broker call and return value.
- **Verifier notes**: Verified: create_fleet (cafleet/src/cafleet/broker/fleets.py:16-21) is create_fleet(label=None, *, director_context, coding_agent), so the variants that omit label (test_administrator.py:17-20, webui/test_api_format.py:31-35, webui/test_monitor_api.py:51-55) are call-equivalent to the shared helper's label=None, and test_inline_preview.py:40-45 passes label=None explicitly. All four build MultiplexerContext(session='main', window_id='@3', pane_id='%0') and coding_agent='claude', matching tests/broker/_helpers.py:7-14 exactly. _FAKE_DIRECTOR_CTX in test_administrator.py is referenced only by _create_fleet_with_ctx (used at lines 101/123/151), so it can go with it. The cross-directory import pattern is established (monitor/test_loop.py:22, output/test_render_broadcast_summary.py:6, plus cli/test_broadcast_*.py already import tests.broker._helpers). The helper resolves broker.create_fleet at call time, so per-test broker_session/api_db monkeypatching of _shared.get_sync_sessionmaker still applies identically. Behavior-preserving.

### [tests-4] 16 identical 3-line _autouse_broker fixtures; 13 of them in tests/broker/ could be one conftest fixture
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/tests/broker`
- **Estimated impact**: ~-48 lines net across 13 files
- **Evidence**: The exact fixture `@pytest.fixture(autouse=True)\ndef _autouse_broker(broker_session): return broker_session` appears 16 times: 13 in tests/broker/ (test_member_activity.py:11, test_monitor.py:36, test_skill_installs.py:14, test_registry.py:16, test_messaging.py:15, test_inline_preview.py:12, test_fleet_bootstrap.py:23, test_broadcast_recipients_delivered.py:22, test_fleet_list_director.py:19, test_to_agent_id_nullable.py:18, test_guard_exit_codes.py:18, test_queries.py:16, test_typed_columns.py:24) plus monitor/test_loop.py:28, output/test_render_broadcast_summary.py:10, webui/test_api_format.py:12. Only broker/test_kind_predicate_safety.py and broker/test_administrator.py (which requests broker_session explicitly) lack it.
- **Proposed change**: Add cafleet/tests/broker/conftest.py containing the single autouse _autouse_broker fixture and delete the 13 in-file copies. (The 3 copies outside tests/broker/ stay, or their directories get the same one-liner conftest.)
- **Why behavior-preserving**: Same fixture body, same autouse activation, same broker_session in-memory DB per test. The only delta is that test_kind_predicate_safety.py would also get an (unused) in-memory sessionmaker patch — broker_session patches get_sync_sessionmaker to a fresh empty DB, which that file never touches, so its tests' observable behavior is unchanged. If even that is unwanted, keep per-file fixtures only for that file's module and still delete 12 copies.
- **Verifier notes**: Verified: exactly 16 copies, every body identical ('return broker_session'), 13 in tests/broker/ at the cited lines plus monitor/test_loop.py:27-29, output/test_render_broadcast_summary.py, webui/test_api_format.py. tests/broker/ has no conftest.py today (only __init__.py). The only broker files without the fixture are test_kind_predicate_safety.py and test_administrator.py, as claimed. test_kind_predicate_safety.py is pure predicate testing (no DB, no sessionmaker access), so the new autouse broker_session adds only per-test setup cost, not observable behavior; test_administrator.py's explicit broker_session parameters (lines 100/122/148) resolve to the same cached instance whether or not an autouse fixture also requests it, and its three DB-free tests are unaffected. broker/test_monitor.py's alembic migration tests already run under the module-level autouse copy, so directory-wide autouse changes nothing for them. Behavior-preserving.

### [tests-5] ~30-line bootstrapped-fleet fixture (DB redirect + tmux stubs + fleet create --json) copy-pasted across 5+ CLI test files
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/tests/cli/test_member.py:29-54`
- **Estimated impact**: ~-100 lines across 6 files
- **Evidence**: The same block — _FAKE_DIRECTOR_CTX constant, monkeypatch of config.settings.database_url to a tmp sqlite file, monkeypatch of TmuxMultiplexer.ensure_available/context_discovery, CliRunner(), _init_registry(), `fleet create --json` + json.loads — is duplicated in cafleet/tests/cli/test_member.py:29-54 (bootstrapped_fleet) and again at 1212-1248 (make_bootstrapped_fleet), test_member_show.py:20-80, test_member_list_all.py:20-80, test_member_list_activity.py:~24-53, test_member_capture_defaults.py:13-57. The tmux-stub half is additionally duplicated as the `_mock_tmux_for_fleet_create` fixture in test_fleet_flag.py:20-31 and test_monitor.py:37-45. Moreover the database_url redirect + _init_registry() inside each copy duplicates what the autouse `_cli_registry` fixture (tests/cli/conftest.py:9-24) already does for every CLI test.
- **Proposed change**: Move to tests/cli/conftest.py: (a) the _mock_tmux_for_fleet_create stub fixture and the shared _FAKE_DIRECTOR_CTX constant, and (b) a `bootstrapped_fleet`-style factory fixture that invokes `fleet create --json` and returns (fleet_id, director_id, admin_id, runner); drop the per-file database_url redirect + _init_registry lines, relying on the autouse _cli_registry. Per-file fixtures then shrink to just their file-specific member registrations.
- **Why behavior-preserving**: Every copy performs the same setup; consolidating produces the same monkeypatches and the same seeded temp registry (autouse _cli_registry already redirects database_url to a fresh tmp path and seeds it via _init_registry before any per-file fixture runs, so dropping the second redirect leaves an equivalently fresh DB). Assertions and CLI invocations are untouched.
- **Verifier notes**: Core claims verified with scope corrections. The shared setup (database_url redirect, TmuxMultiplexer.ensure_available/context_discovery stubs, CliRunner + _init_registry + fleet create --json) is duplicated at all cited locations, and the key redundancy claim is sound: engine singletons (db/engine.py:32-46) are built lazily on first broker/CLI call and run_db_init uses its own ad-hoc engine, so the autouse _cli_registry redirect+seed (which runs before per-file fixtures) makes the per-file redirect + _init_registry equivalent — dropping them leaves an identically fresh, migrated, seeded per-test DB. Corrections: (1) the copies are NOT 'the same block' — returns differ (test_member.py bootstrapped_fleet: 3-tuple without admin_id; member_show/member_list_all: 6-tuples in different orders; capture_defaults: 5-tuple of STRINGS; make_bootstrapped_fleet is a factory with an extra HOME redirect and --coding-agent handling that must be preserved), so the proposed single (fleet_id, director_id, admin_id, runner) fixture matches no existing call site verbatim and per-file wrappers/edits are required; (2) cli/test_monitor.py's fresh_db returns db_file for direct SQL seeding (_seed_runtime), so if its redirect is dropped those reads must switch to _cli_registry's returned db_path; (3) the relocated _mock_tmux_for_fleet_create should stay opt-in (requested by the files that use it) rather than dir-wide autouse to avoid altering the baseline of tests exercising tmux-absence paths. With those adaptations the consolidation preserves behavior.

### [tests-6] test_should_ping__due_when_never_pinged duplicates test_should_ping__last_ping_none_due_immediately exactly
- **Status**: CONFIRMED
- **Kind**: redundant-test
- **Location**: `cafleet/tests/monitor/test_should_ping.py:36-38, 94-96`
- **Estimated impact**: -4 lines, one fewer duplicate test
- **Evidence**: Line 36-38: `def test_should_ping__due_when_never_pinged(): assert should_ping(_target(), _NOW) is True`. Line 94-96: `def test_should_ping__last_ping_none_due_immediately(): assert should_ping(_target(last_ping_at=None), _NOW) is True`. The _target factory (lines 18-33) defaults last_ping_at=None, so both build the byte-identical dict and make the byte-identical assertion. The first half of test_should_ping__pinged_regardless_of_pending_count (line 52: `should_ping(_target(pending_count=0), _NOW) is True`) is a third copy of the same call, since pending_count defaults to 0.
- **Proposed change**: Delete test_should_ping__due_when_never_pinged (lines 36-38). Keep test_should_ping__last_ping_none_due_immediately, whose name and comment state the NULL-last_ping contract explicitly. Optionally drop the redundant first assert in test_should_ping__pinged_regardless_of_pending_count, keeping the pending_count=5 assert that actually varies the input.
- **Why behavior-preserving**: The retained test test_should_ping__last_ping_none_due_immediately asserts the identical expression on the identical input, so coverage of the never-pinged-is-due contract is unchanged.
- **Verifier notes**: Verified: _target() (tests/monitor/test_should_ping.py:18-33) defaults last_ping_at=None and pending_count=0, so lines 36-38, 94-96, and the first assert of test_should_ping__pinged_regardless_of_pending_count (line 52) all evaluate should_ping on the byte-identical dict against the same _NOW and assert the same result. Deleting lines 36-38 (and optionally line 52's first assert) leaves the never-pinged-is-due contract covered by the retained test, whose line-95 comment states it explicitly. No doc, skill, SPEC, or design file references either test name. Behavior-preserving.

### [tests-7] Four deregister-guard tests in test_fleet_flag.py inline the setup their own db_runner fixture already provides
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/tests/cli/test_fleet_flag.py:204-319`
- **Estimated impact**: ~-32 lines within one file
- **Evidence**: test_deregister_administrator_cli_guard__cli_deregister_admin_exits_nonzero (204-214), __cli_deregister_admin_message_is_user_friendly (232-242), __cli_deregister_unknown_agent_exits_nonzero (262-272), and __cli_deregister_admin_leaves_row_active (292-302) each repeat the same 8 lines: db_file = tmp_path/"cafleet.db"; monkeypatch.setattr(config.settings, "database_url", ...); runner = CliRunner(); _init_registry(). The file's own db_runner fixture (lines 34-44) performs exactly this, and the autouse _cli_registry (tests/cli/conftest.py:10-24) already returns a seeded temp db_path usable by _fetch_agent_status.
- **Proposed change**: Have the three tests that never touch db_file take `db_runner`; have __cli_deregister_admin_leaves_row_active take `db_runner` plus `_cli_registry` (whose return value is the db_path) for _fetch_agent_status — or extend db_runner to return (runner, db_file). Delete the four inline setup blocks.
- **Why behavior-preserving**: The fixture performs the identical redirect-to-tmp-sqlite + _init_registry sequence the inline blocks perform; the tests' CLI invocations, exit-code and SQL assertions are unchanged.
- **Verifier notes**: Duplication facts verified: the four tests (lines 204-214, 232-242, 262-272, 292-302) each repeat the exact redirect + CliRunner + _init_registry sequence that db_runner (lines 34-44) performs, and _autouse_reset_engine/_mock_tmux_for_fleet_create are module-autouse so fixture-based tests behave identically. IMPORTANT correction — the first proposed mechanic for the fourth test is REFUTED: taking 'db_runner plus _cli_registry' does not work, because db_runner re-redirects database_url to its own tmp_path/'cafleet.db', so _cli_registry's returned db_path (tmp_path/'cli-registry'/'cafleet.db') points at a different, fleetless DB and _fetch_agent_status(db_path, admin_id) would hit the 'agent not found' assert (test_fleet_flag.py:200). Only the alternative preserves behavior: extend db_runner to return (runner, db_file) (updating its other call sites), or drop db_runner for that test and rely on _cli_registry alone (runner = CliRunner(), reading _cli_registry's db_path). The three tests that never touch db_file can simply take db_runner.

### [tests-8] Member placement-dict builder and _register_monitoring_member re-implemented in 3-6 test files
- **Status**: CONFIRMED
- **Kind**: duplication
- **Location**: `cafleet/tests/broker/test_monitor.py:40-69`
- **Estimated impact**: ~-60 lines across 6-7 files
- **Evidence**: The dict {"director_agent_id": <director>, "tmux_session": "main", "tmux_window_id": "@3", "tmux_pane_id": <pane>, "coding_agent": "claude"} is built by near-identical local _placement helpers in cafleet/tests/broker/test_monitor.py:40-47 and cafleet/tests/monitor/test_loop.py:32-39, and inlined verbatim in webui/test_monitor_api.py:58-70, broker/test_inline_preview.py:48-60, cli/test_member_show.py:55-79, cli/test_member_list_all.py:55-79, cli/test_member_capture_defaults.py:42-53. _register_monitoring_member (register_agent with kind="monitoring-member", description="monitoring member") is triplicated at broker/test_monitor.py:59-69, monitor/test_loop.py:49-57, webui/test_monitor_api.py:73-80+.
- **Proposed change**: Add `_member_placement(director_agent_id, pane_id, coding_agent="claude")` and `_register_monitoring_member(fleet, name, pane_id)` to tests/broker/_helpers.py (which already hosts _create_fleet/_register_agent and is imported cross-directory by monitor/, output/ and cli/ tests) and replace the local copies/inline dicts.
- **Why behavior-preserving**: Every copy produces the same placement dict keys/values and the same register_agent call; centralizing the builder changes no inputs to the code under test and no assertions.
- **Verifier notes**: Verified with scope corrections. The placement dict ({director_agent_id, tmux_session:'main', tmux_window_id:'@3', tmux_pane_id, coding_agent:'claude'}) is uniform across all cited sites (pane id and director id parameterized), and _register_monitoring_member (description='monitoring member', kind='monitoring-member') is genuinely triplicated at broker/test_monitor.py:59-69, monitor/test_loop.py:49-57, webui/test_monitor_api.py:73-85. Corrections: (1) the three monitoring-member copies differ in return type — test_monitor returns the full dict, test_loop and test_monitor_api return ['agent_id'] — and in defaults ('watcher'/'%5' vs required args vs 'watcher'/'%7'), so centralizing requires one canonical return plus call-site adjustments; (2) 'the same register_agent call' is inaccurate for the cli inline sites: member_show/member_list_all/capture_defaults use different names, kinds, and descriptions ('Dedicated monitoring member', 'Test member', 'Ordinary member', 'member to capture from'), and tests assert on those exact literals and pane ids (e.g. test_member_show.py:192 asserts pane %10; list/show output includes names), so only the placement builder — parameterized by director id and pane — is shareable there, with each site keeping its own register_agent kwargs. Executed with those parameters, the consolidation changes no inputs to the code under test.
