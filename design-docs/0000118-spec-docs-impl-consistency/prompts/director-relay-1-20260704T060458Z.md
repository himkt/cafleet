User answers to your 4 clarifying questions:

Q1 (to_agent_id, items 1.1/1.2): Align CODE to SPEC (option B). Migrate the schema to a nullable to_agent_id, write NULL (not 0) on broadcast_summary rows, and change both surfacing checks (queries.py:68-70 and formatters.py:39-40) from truthiness to `is None` / `IS NULL`. This needs a NEW Alembic migration; `--json` output changes 0 -> null. The docs/SPEC §5.5 already describe this target state, so they stay as-is (they become correct once the code matches). Items 1.1 and 1.2 are therefore CODE fixes, not doc edits.

Q2 (item 3.5, hidden flags): Do NOT blanket-pick unhide-vs-document. Instead, for EACH hidden flag (--full, --quiet, --activity, --ansi/--no-ansi), first assess whether the flag is genuinely needed/used:
  - If a flag is NOT genuinely needed -> the design doc MUST specify REMOVING it entirely (code + every doc mention, per the project removal rule .claude/rules/removal.md), not hiding or documenting it.
  - If a flag IS needed -> treat it as a CODE fix: unhide it (remove hidden=True) so it honors SPEC §10 "no hidden flags", and cli-options.md's "documented" claim becomes correct as-is.
  So each hidden flag gets exactly one disposition in the design doc: REMOVE (if unneeded) or UNHIDE (if needed). Include your per-flag recommendation with a one-line rationale, and note where you are uncertain so the Reviewer/user can check.

Q3 (items 2.2 & 3.3, broker-guard exit codes): Normalize BOTH broker guards to raise ClickException (exit 1) — option A. This matches the sibling guard at member.py:328-331 and satisfies both docs (which already expect exit 1) with NO doc edit. Both 2.2 and 3.3 become CODE fixes.

Q4 (scope): Option A — actionable tasks. Produce ONE executable design doc whose Implementation section lists BOTH the doc-alignment edits AND the code fixes (with their test updates) as concrete, ordered, directly-executable tasks for the execute cycle. Every one of the 32 contradictions must be covered with an exact edit (file, line, and doc-side-vs-code-side direction).

Proceed to draft the design doc at the OUTPUT PATH. Follow the project's documentation-maintenance rule (SPEC.md + docs/ are first-class targets) and the cafleet-design-doc template. When the draft is complete, send `complete (doc)`.
