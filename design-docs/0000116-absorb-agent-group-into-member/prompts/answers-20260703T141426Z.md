User answers (via Director relay):

Your understanding of the already-decided direction is correct on all five bullets.

Q1 (paneless external-agent concept): (a) DEAD. No user-facing way to create a paneless agent remains. broker.register_agent(placement=None) stays as internal machinery only (fleet bootstrap, WebUI reads). Delete the self-registration bootstrap workflow in skills/cafleet/reference/cli.md outright. Messages-to-paneless degenerates to the built-in Administrator; no broker change. Restoration plan for a future A2A/HTTP registration lives only in this design doc.

Q2 (introspection surface): (a) BOTH. member list --all (adds root Director, Administrator, and placementless rows, with kind/backend indication) PLUS a new member show --member-id <id> (full detail: description, kind, skills, status, placement block, --full semantics like today's agent show).

Q3 (member show flag shape): YES. Bare --member-id <target> only; drop the requester gate — consistent with the rest of the member group.

Q4 (drift guard): (c) NO new guard patterns. User's explicit reasoning: the removal rule demands removing all mentions, and a guard pattern string is itself a standing mention of the dead command — so no "cafleet agent" pattern is added to spawn_prompt_guard.py. One-time migration only. Do not add any anti-"cafleet agent" assertion anywhere.

Q5 (WebUI): YES, minimal. Only the Dashboard empty-state string changes (point at cafleet member create); HTTP API and everything else in the WebUI untouched.

Baked-in assumption: APPROVED. Regression test asserting cafleet agent ... fails with Click's "No such command" is in scope, and cafleet/tests/cli/test_agent.py is replaced by member-side coverage (member show / list --all / delete paneless soft-delete).

Additional user emphasis from the original request (restated): the documentation surface is large — enumerate it exhaustively in the Implementation steps (docs/ incl. spec/cli-options.md + concepts pages, SPEC.md, README.md, every affected skills/**/SKILL.md + reference/*.md + roles/*.md, project-local .claude/rules/), documentation-first per .claude/rules/documentation-maintenance.md.

Proceed to draft.
