[project]
name = "cafleet-root"
version = "0.0.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["./cafleet"]

[dependency-groups]
dev = [
    "bump-my-version>=1.3.0",
]
research = [
    "matplotlib>=3.10.8",
]
