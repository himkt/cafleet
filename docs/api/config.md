---
icon: lucide/code
---

# config

The settings surface: every `CAFLEET_*` environment variable resolves to a
field on the `Settings` model defined here, with its default and aliases.

::: cafleet.config
